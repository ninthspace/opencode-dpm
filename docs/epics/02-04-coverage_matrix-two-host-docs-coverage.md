# Coverage — Documentation and permissions

**Number**: 02-04  
**Source epic**: 02-04  
**Status**: pending  

## Coverage

| # | Requirement | Spec Text | Story Criterion | Covered by | Test Approach | Verified |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | FR6 | README install, first run, guard symlink and "when the guard refuses" cover v1 and v2 | The README's account is correct for the host DPM supports and complete: for install, first run, guard symlink and "when the guard refuses", the instruction a reader actually follows; for Status, the host, the specs built out and the unfinished items as they now stand. Control: a planted section with its instruction removed and a planted Status naming a host DPM does not run under, each reported by heading. Amended from "gives both the v1 and the v2 form": the delivered README states there is no other host DPM runs under, so the per-host pair has no second member and the form was a mechanism for the property rather than the property. Status is named here because Chris folded it into this story at the epic's change moment — it carries the port's most visible stale claims and is the section a reader reaches when deciding whether to trust the thing. | Story 1 | `[unit]` |  |
| 2 | FR6 | with the plugin-package location correct for each host rather than one location presented as the location | Where DPM lives on disk is stated as what it now is — a clone whose path the reader chose — and no cached-package path is presented as the location. Control: a planted package-cache sentence, reported by line. Amended from "stated per host, so neither host's path is presented as the location": there is one host, and since epic 02-01 there is no packaged install either, so the failure to guard against is not two locations competing for the definite article but a retired one surviving. Retro 04's lesson applies in its stronger form — find the instructions that DERIVE something from the install location's shape, not only those that print it. | Story 1 | `[unit]` |  |
| 3 | FR6 | rather than one location presented as the location | must NOT — The README names a host binary or a host version DPM does not run under — `opencode2`, or a v2 claim about the port, its behaviour or what it was built against. Control: a planted `opencode2` and a planted "ported to OpenCode v2", each reported by line. Amended from "names one host's binary where the sentence is true of either": with one supported host there is no sentence true of either, and the live failure is the opposite one — a second host's name surviving the pivot that removed it. The sweep is over the README and over prose, not over the tree: `opencode2` is correct history in four test doc comments and in the protected v0.7.0 dump fixture, and a whole-tree reading would be red for reasons that are all right. | Story 1 | `[unit]` |  |
| 4 | FR7 | The recommended entries are stated per host, against what that host actually matches on | The permission rule the README recommends matches all twenty-three skill identities as the host's permission engine sees them — one `dpm-*` form, which works because ADR 02-04 put the prefix in the front-matter `name` the engine reads rather than in a registered id it does not. Matched against the transcribed list of published ids rather than against whatever the tree discovers, with the discovered set held equal to that list separately, so the two disagreeing is itself a failure. Control: a planted rule matching none, reported as a failure rather than passing over an empty match set, which is the shape that passes by doing nothing. Amended from "on both hosts": there is one host, and the clause was the reason the rule needed defending rather than the thing being defended. | Story 2 | `[unit]` | ✓ |
| 5 | FR7 | v1's permission engine evaluates the `skill` action against the unprefixed front-matter name | The README states what the permission engine matches the `skill` action against — the front-matter `name` — so a reader can tell why the `dpm-*` rule works instead of taking it on trust, and the section carries no sentence contradicted by what DPM now does. Amended from "per host … v1 the front-matter name, v2 the registered id, so a reader can tell why one rule works on both": with one host there is no "both" for a reader to reconcile, and the sentence's job moves from reconciling two engines to explaining one. The second clause is the amendment's substance rather than a flourish — epic 02-03 removed the `external_directory` rule and left the two paragraphs that framed it, so the section now opens by telling a reader to set a rule it later says is unnecessary. | Story 2 | `[unit]` |  |
