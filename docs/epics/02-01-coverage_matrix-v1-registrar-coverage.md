# Coverage — The v1 registrar

**Number**: 02-01  
**Source epic**: 02-01  
**Status**: pending  

## Coverage

| # | Requirement | Spec Text | Story Criterion | Covered by | Test Approach | Verified |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | FR1 | ~~the one appropriate to the host runs for the host that loaded it~~ | ~~A single named predicate classifies the host from the plugin context it is given, and the exported entry hands off to one of two registrars on its result.~~ | Story 2 | `[unit]` | **Withdrawn 2026-08-31T21:20:00.000Z** — The criterion this bound was superseded: Overtaken by Chris's decision on 2026-08-31 to target OpenCode v1 alone ("given we can't currently work with a v2 of opencode, let's concentrate on v1"), taken at an explicit gate that named this story's verified work as the cost. With one host there is no host to classify: `src/plugin/host.ts` and its `hostOf` predicate are deleted, and so is the two-registrar dispatch this criterion measured. What replaced it is two modules, one per v1 protocol, both loaded unconditionally — nothing classifies anything at run time. The criterion is not merely unmet, it names a mechanism that no longer exists. |
| 2 | FR1 | ~~A single documented install route~~ | ~~The tools module exports a callable `server` and no default, and the skills module exports a default object and no callable `server`, so the two v1 protocols stay in two files. Control: the absence is read off the module namespace rather than through a default import, which would be a parse error rather than a failing assertion.~~ | Story 3 | `[unit]` | **Withdrawn 2026-08-31T20:14:02.470Z** — Bound in error. The criterion establishes the two modules' export shapes, which is what makes two entries necessary; it does not establish that the route is documented. FR1's install-route fragment needs the README, which still says `opencode2 plugin add` and names one entry. |
| 3 | FR1 | the MCP server registered and connected, all twenty-three skills advertised, and nothing further for the user to copy into the project | One load of both plugin entries against the recorded v1 context produces a configuration holding `mcp.dpm` and a skill registry holding twenty-three embedded sources, with the server command and every skill location resolved from the same package root rather than from two independent constructions. | Story 5 | `[integration]` | ✓ |
| 4 | FR1 | ~~the one appropriate to the host runs for the host that loaded it~~ | ~~control — The predicate's test fails when given a context both branches would match and when given one neither would, so a classification that has stopped discriminating is caught rather than silently defaulting.~~ | Story 2 | `[unit]` | **Withdrawn 2026-08-31T21:20:00.000Z** — The criterion this bound was superseded: Superseded with 01M1C9B3FR9TCS7YFXNV02SN1Q, whose predicate it was the control on. A control on a classification that no longer happens has nothing to be a control on. |
| 5 | FR1 | the user does not choose a registrar | must NOT — Registration requires the user to name a host, choose between entry points, or set an option. Control: a planted second required argument on the exported entry fails the check. | Story 2 | `[unit]` | ✓ |
| 6 | FR1 | ~~all twenty-three skills advertised~~ | ~~The v1 registrar appends the package's own skills directory to `config.skills.paths`, and discovery over that directory finds exactly twenty-three `SKILL.md` files, every one resolving to a path inside the installed package.~~ | Story 4 | `[unit]` | **Withdrawn 2026-08-31T21:45:00.000Z** — The criterion this bound was superseded: `config.skills.paths` is not the route, and does not need to be. Probing v1 1.18.25 found `skill.transform` on the object-route context, whose draft takes `{type:'embedded', skill}` sources carrying the computed skill rather than a path to it — a typed, published API where `skills.paths` is absent from both the SDK types and the live schema. The replacement criterion measures the same outcome, twenty-three skills each resolving inside the installed package, against the mechanism actually used. |
| 7 | FR1 | ~~The package registers itself with a v1 host~~ | ~~One plugin load under a stub v1 context produces a configuration holding both `mcp.dpm` and dpm's `skills.paths` entry, built from the same computed inputs rather than from two independent constructions.~~ | Story 5 | `[integration]` | **Withdrawn 2026-08-31T23:00:00.000Z** — The criterion this bound was superseded: Names `skills.paths`, which is not the route. v1 registers skills through `skill.transform`'s embedded sources, and the two registrations come from two modules rather than one load. Replaced by the criterion that asserts both entries resolving from one package root. |
| 8 | FR1 | A single documented install route, and the user does not choose a registrar — the one appropriate to the host runs for the host that loaded it. | The tools module exports a callable `server` and no default, and the skills module exports a default object and no callable `server`, so the two v1 protocols stay in two files. Control: the absence is read off the module namespace rather than through a default import, which would be a parse error rather than a failing assertion. | Story 3 | `[unit]` | ✓ |
| 9 | FR2 | ~~Registration goes through v1's `config` hook, setting a local server entry whose command runs the packaged executable~~ | ~~Given a stub v1 plugin context, the v1 registrar's `config` hook sets `mcp.dpm` to a local-server entry whose command runs the packaged executable.~~ | Story 3 | `[unit]` | **Withdrawn 2026-08-31T19:03:30.620Z** — The criterion it binds was superseded. Probes against opencode 1.18.25 establish that v1's `config` hook is reachable only from a module with NO default export: with a default present v1 calls `default.setup(context)` and never looks at the named `server` export (observed, with the default's own call as the control in the same run). v2 requires a default export object and ignores a callable one entirely. So the mechanism FR2 names cannot coexist with v2 support in one entry file. Chris decided on 2026-08-31, twice and with FR2's text in front of him, that v1 registers skills and not tools. FR2 is therefore left undischarged rather than bound to a criterion that cannot be met, and amending FR2 is a separate decision outside this run. |
| 10 | FR2 | Registration goes through v1's `config` hook, setting a local server entry whose command runs the packaged executable | v1's `config` hook sets `mcp.dpm` to a local-server entry of type `local` whose command runs the packaged executable, and the user's own `mcp` entries survive the registration unchanged. Control: a load against an empty `mcp` block shows the reading would notice a user entry that had been dropped. | Story 3 | `[integration]` | ✓ |
| 11 | FR2 | ~~the same `{type:"local", command, environment?}` shape the v2 registrar already builds~~ | ~~The local-server entry the v1 registrar builds is identical to the one the v2 registrar builds — same command array, same environment.~~ | Story 3 | `[unit]` | **Withdrawn 2026-08-31T19:03:37.783Z** — The criterion it binds was superseded alongside 01M1C9C16CF1HKKNM8TZ304BF9. With no MCP server registered under v1 there is no v1-built local-server entry to compare against the v2 one, so the fragment has nothing left to bind to. The shape claim itself survives untouched in story 2: `registrationInputs` computes ONE server entry above the host branch and hands the same object to whichever registrar runs, which is verified by coverage row 01M1C9J4DRYB598ZF5H5ZG4R1S. |
| 12 | FR2 | the same `{type:"local", command, environment?}` shape | The entry the hook registers is the one `localServer` builds — same type, same command array — rather than a second construction of the path that agrees today. Each load is handed its own copy, so a host mutating what it was given cannot change what the next load reads. | Story 3 | `[unit]` | ✓ |
| 13 | FR2 | no host-specific tool exists | must NOT — Any tool is advertised under one host and not the other. Control: a planted host-conditional tool registration fails the check. | Story 3 | `[unit]` | ✓ |
| 14 | FR2 | setting a local server entry whose command runs the packaged executable | The MCP command the config hook registers is one a v1 host can actually spawn: it names a runtime that carries `node:sqlite`, so the server starts and answers rather than dying on a missing built-in module while the host waits for it. | Story 5 | `[integration]` | ✓ |
| 15 | FR2 | setting a local server entry whose command runs the packaged executable | control — The reading that accepts the registered command would reject the one it replaced: handed a bun runtime, the same check refuses rather than passing, so "the command is startable" is a property being measured rather than an assertion nothing could contradict. | Story 5 | `[unit]` |  |
| 16 | FR3 | ~~each skill resolves with its `location` inside the installed package~~ | ~~The v1 registrar appends the package's own skills directory to `config.skills.paths`, and discovery over that directory finds exactly twenty-three `SKILL.md` files, every one resolving to a path inside the installed package.~~ | Story 4 | `[unit]` | **Withdrawn 2026-08-31T21:45:00.000Z** — The criterion this bound was superseded: `config.skills.paths` is not the route, and does not need to be. Probing v1 1.18.25 found `skill.transform` on the object-route context, whose draft takes `{type:'embedded', skill}` sources carrying the computed skill rather than a path to it — a typed, published API where `skills.paths` is absent from both the SDK types and the live schema. The replacement criterion measures the same outcome, twenty-three skills each resolving inside the installed package, against the mechanism actually used. |
| 17 | FR3 | ~~each skill resolves with its `location` inside the installed package~~ | ~~The skills module registers all twenty-three skills as `embedded` sources through `skill.transform`, each carrying a non-empty body and a `location` inside the installed package. Control: the source list is driven against a draft offering `source` and `list` and not `add`, so a registration written against the v2 API fails rather than passing against a double more generous than the host.~~ | Story 4 | `[integration]` | **Withdrawn 2026-09-03T12:18:11.103Z** — The criterion bound here asserts the skills module registers twenty-three skills as `embedded` sources through `skill.transform`. Epic 02-05 story 2 established that v1's plugin `Hooks` has no skill member and deleted that entry; the mechanism does not exist, so the binding cannot be re-verified and FR3 now says in as many words that there is no v1 skills registrar. |
| 18 | FR3 | ~~registered rather than copied — holds unchanged under v1~~ | ~~The skills module registers all twenty-three skills as `embedded` sources through `skill.transform`, each carrying a non-empty body and a `location` inside the installed package. Control: the source list is driven against a draft offering `source` and `list` and not `add`, so a registration written against the v2 API fails rather than passing against a double more generous than the host.~~ | Story 4 | `[integration]` | **Withdrawn 2026-09-03T12:18:14.573Z** — Same criterion as the row above, and gone for the same reason: it names the `skill.transform` registration epic 02-05 deleted. The property the fragment quotes — registered rather than copied — still holds under the `skills` configuration key, but nothing live asserts it, which is the gap this retirement makes visible instead of hiding. |
| 19 | FR3 | ~~each skill resolves with its `location` inside the installed package so directory-based skills keep their supporting files~~ | ~~One load of both plugin entries against the recorded v1 context produces a configuration holding `mcp.dpm` and a skill registry holding twenty-three embedded sources, with the server command and every skill location resolved from the same package root rather than from two independent constructions.~~ | Story 5 | `[integration]` | **Withdrawn 2026-09-03T12:18:17.309Z** — The criterion asserts one load of *both* plugin entries produces a skill registry of twenty-three embedded sources. The second entry was deleted in epic 02-05 story 2 — there is no arrangement of exports by which a v1 plugin registers a skill — so the criterion describes a load that cannot happen. |
| 20 | FR3 | ~~so directory-based skills keep their supporting files~~ | ~~Every `dpm/shared/*.md` reference in every registered body has been rewritten to an absolute path that exists on disk, so a session reading a skill's conventions reaches the file rather than a path that resolves against nothing. Control: a body naming a shared file that is not in the package is refused at registration rather than registered with a broken path.~~ | Story 4 | `[integration]` | **Withdrawn 2026-09-03T12:18:19.237Z** — The criterion asserts every `dpm/shared/*.md` reference is rewritten to an absolute path at registration. ADR 02-01 replaced that rewrite with the shared-document MCP tool and epic 02-03 story 3 deleted `resolveSupportingPaths`, so the criterion names a registration-time transform the package no longer performs. |
| 21 | FR5 | dpm's skills survive v1's flat name keyspace | Every registered skill's `name` carries the `dpm-` prefix, because `name` is the only field v1 keeps of an embedded skill and so is the whole of its keyspace. Control: the reading recognises an unprefixed name, so the empty result is a finding rather than a filter over nothing. | Story 4 | `[unit]` | ✓ |
| 22 | FR5 | a collision is not silent | Registering over a name another embedded source already claimed reports that name rather than shadowing it silently. Control: a draft holding no clashing name produces no report, so the notice is proof of a clash rather than of a logger that always fires. The report states its own reach: a clash behind a `directory` or `url` source is not visible at this point and the prefix is the whole of the defence there. | Story 4 | `[unit]` | ✓ |
| 23 | FR6 | README install, first run, guard symlink and "when the guard refuses" cover v1 | The README documents the install this epic actually delivers — both entry files named, the clone route given, and the reason the packaged route is not offered stated where a reader meets it — and every runnable block it gives is executed by the suite rather than transcribed into it. | Story 5 | `[integration]` |  |
| 24 | FR6 | ~~with the plugin-package location correct for each host rather than one location presented as the location~~ | ~~The README documents the install this epic actually delivers — both entry files named, the clone route given, and the reason the packaged route is not offered stated where a reader meets it — and every runnable block it gives is executed by the suite rather than transcribed into it.~~ | Story 5 | `[integration]` | **Withdrawn 2026-09-12T14:13:20.375Z** — The fragment's "correct for each host rather than one location presented as the location" is a per-host contrast with no second member once FR6 documents one host. Amended out by this pivot and rebound to "Where DPM lives on disk is stated as what it is — a clone whose path the reader chose — rather than a retired packaged path presented as the location", which carries the same obligation against the install DPM actually has. |
| 25 | FR6 | Where DPM lives on disk is stated as what it is — a clone whose path the reader chose — rather than a retired packaged path presented as the location | The README documents the install this epic actually delivers — both entry files named, the clone route given, and the reason the packaged route is not offered stated where a reader meets it — and every runnable block it gives is executed by the suite rather than transcribed into it. | Story 5 | `[integration]` |  |
| 26 | NFR1 | ~~host-specific code is confined to the registration layer~~ | ~~The predicate's identifier appears only in the registration layer and its own test. Control: a planted use elsewhere under `src/` fails the check.~~ | Story 2 | `[unit]` | **Withdrawn 2026-08-31T21:20:00.000Z** — The criterion this bound was superseded: Superseded with the predicate it names. `hostOf` is deleted, so a rule about where its identifier may appear has no subject. NFR1's underlying claim — that host-specific code is confined to the registration layer — is not lost: it moves to story 3's must-NOT sweep, which reads the same corpus with the same planted control and now carries the NFR1 binding. |
| 27 | NFR1 | host-specific code is confined to the registration layer | must NOT — Any tool is advertised under one host and not the other. Control: a planted host-conditional tool registration fails the check. | Story 3 | `[unit]` | ✓ |
| 28 | NFR1 | ~~Skill bodies, tool schemas, the MCP server, the guard and the database are shared verbatim between hosts~~ | ~~Both registrars are given the same skill list and the same server entry, each computed by one call, rather than by two constructions that happen to agree.~~ | Story 2 | `[unit]` | **Withdrawn 2026-08-31T21:20:00.000Z** — The criterion this bound was superseded: Superseded by the v1-only retarget. There are no registrars to give one thing to: dpm now ships two plugin modules, one per v1 protocol, and they register different things — the tools module a server entry, the skills module a skill list. The claim worth keeping from this criterion is narrower and survives in `src/plugin/registration.ts`, where both modules resolve the same package root through `packageRoot`, and it is verified by story 3's criterion on the registered entry being the one `localServer` builds. |
| 29 | NFR1 | ~~One codebase, no per-host fork~~ | ~~The same exported entry loaded under a stub v2 context produces the v2 registrations and writes no `config` at all.~~ | Story 5 | `[integration]` | **Withdrawn 2026-08-31T23:00:00.000Z** — The criterion this bound was superseded: Measures behaviour under a v2 context. v2 was dropped from this epic by explicit decision, so there is no second host to compare against. Replaced by the criterion that the registered MCP command is one a v1 host can actually spawn — the defect this story's integration check found. |
| 30 | NFR2 | v1 and v2 publish the same package name at different versions | `@opencode-ai/plugin-v1` resolves to `@opencode-ai/plugin@1.18.25` through an npm alias declared in `devDependencies`, and `tsc --noEmit` exits zero with both SDK versions in the type graph. | Story 1 | `[unit]` | ✓ |
| 31 | NFR2 | `dependencies` stays empty | `dependencies` in `package.json` is empty and both SDK entries sit under `devDependencies`. Control: a planted runtime entry fails the check. | Story 1 | `[unit]` | ✓ |
| 32 | NFR2 | the existing module sweep proving nothing under `src/` imports a package at runtime | Nothing under `src/` imports a package at runtime, with the module sweep extended to recognise the aliased specifier as an SDK import. Control: a planted value import of either SDK fails the sweep. | Story 1 | `[unit]` | ✓ |
| 33 | NFR3 | ~~from exactly one place, named as unsupported where a maintainer will read it~~ | ~~`skills.paths` is written from exactly one module, and that module carries a marker naming the key as undocumented by OpenCode and saying what to do when it stops working. Control: a planted second write site fails the check.~~ | Story 4 | `[unit]` | **Withdrawn 2026-08-31T21:45:00.000Z** — The criterion this bound was superseded: There is no undocumented key to fence. NFR3 required `config.skills.paths` to be written from exactly one module and marked as unsupported; the embedded-source route means it is written from none. The requirement's premise dissolved rather than being satisfied, which is reported at the epic roll-up rather than claimed as coverage. |
| 34 | NFR3 | ~~with a check that fails when it stops working~~ | ~~The post-registration check reports failure when given a host configuration that does not hold dpm's skills path, and passes when given one that does.~~ | Story 4 | `[unit]` | **Withdrawn 2026-08-31T21:45:00.000Z** — The criterion this bound was superseded: The post-registration check existed because a configuration key the host silently normalises away registers nothing while appearing to work. `skill.transform` fails at the call instead: the draft's `source` decodes what it is given and throws on a shape it does not accept, which is how the embedded variant was established in the first place. There is no silent-no-op failure mode left for a check to detect. |
| 35 | NFR5 | The second registrar adds no compile, no loader and no published artefact | must NOT — Any invocation passes a loader flag, a transpiler flag, or any other runtime argument beyond the entry path. Control: a planted `--import` in a package script fails the check. | Story 1 | `[unit]` | ✓ |
| 36 | NFR5 | `tsc --noEmit` remains a type check rather than a build | There is no build script and no compiled artefact; `tsc --noEmit` remains a check rather than a step that produces output. Control: a planted build script fails the check. | Story 1 | `[unit]` | ✓ |
| 37 | ENVX3 | copying skill files into the user's project tree must not be required | must NOT — Registration writes anything into the user's project tree. Control: the project tree is hashed before and after a plugin load and compared, with a planted write proving the comparison fires. | Story 4 | `[integration]` | ✓ |
| 38 | ENVX4 | the plugin must not write to the user's OpenCode configuration | must NOT — The plugin writes to the user's OpenCode configuration file. Control: the configuration files are hashed across a plugin load and compared, with a planted write proving the comparison fires. Amended by the spec 02 pivot of 2026-09-03: where the hashes differ, the difference is attributed before the criterion is judged. 1.18.25 inserts a `"$schema"` member into `opencode.json` on load, and a bare hash comparison reports the host's own write as dpm's — so the comparison locates a write and does not, on its own, say whose it was. | Story 3 | `[integration]` |  |
