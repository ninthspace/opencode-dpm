# Compact Summary (PostCompact)
**Captured**: 2026-08-31 00:12:25
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Starting state:** The session was resumed from a compaction. The prior summary established this is the OpenCode v2 port of dpm at `/Users/chris/Work/git/opencode-dpm`, a standalone fork of dpm v0.7.0. The in-flight task was a `dpm:retro` run in synthesise mode, with startup nearly complete (session created, Library Check done — the one library document scoped to architect/do/epics/spec, not retro, so it did not apply).

**Phase 1 — Retro subject selection.** I called `list_observation` (limit 80) and `list_quick` (empty). Got 34 observations. Then `list_epic` (5 epics, all complete) and `list_story` for epics 01-02 through 01-05 to map story_ids to epics. Discovered four groups of ungathered observations, not three: epic 01-02 has retro 02, but retro 02's four observations carry `story_id: null`, so its five stories' six observations were never gathered. Presented four candidates via AskUserQuestion. Chris chose **01-03 Skill port**.

**Phase 2 — Retro 03 (epic 01-03).** Fetched 7 observations with bodies across 5 stories, their categories, and taxonomy (7 terms, limit 100, none project-added). Categories: complexity-underestimates (2), patterns-worth-reusing (2), codebase-discoveries (1), scope-surprises (1), criteria-gaps (1). Wrote two syntheses for the paired categories, left the three singletons unset. Gate approved. Created retro `01M1ABMF2GJVS45PYQ2H6WCVW1` slug `skill-port-instruments`, title "Skill port: the cost landed in the instruments". Updated 7 observations with retro_id, positions 0-6, synthesis on 4. Published. Session updated.

**Phase 3 — Mid-turn "Next retro".** Chris sent "Next retro" while I was working. I published retro 03's projection, then asked which of the remaining three (01-04, 01-05, 01-02 repair, or all three). Chris chose **01-04 Guard and docs**.

**Phase 4 — Retro 04 (epic 01-04).** Fetched 5 observations with bodies. Found story 2's observation (`01M19ZHE2YSQB357FDEPCS9355`) had **no categories at all** — verified not a paging artefact (limit 100, more:false). Proposed adding codebase-discoveries + patterns-worth-reusing. Wrote three syntheses (codebase-discoveries ×3, testing-gaps ×2, patterns-worth-reusing ×2), criteria-gaps singleton unset. Noted the constraint that `synthesis` is one column, so rows in two paired categories get both paragraphs. Gate approved ("Approve"). Created categories, created retro `01M1ABW515ZXDD7XEVEANRSWH0` slug `guard-and-docs-second-surface`. Updated 5 observations, positions 0-4.

**Phase 5 — Second mid-turn "Next retro".** I reasoned that "next retro" now unambiguously means 01-05 (the only remaining epic without a retro; 01-02 is a repair, not a retro), so proceeded without a gate. Fetched 3 observations + categories. Two paired categories: scope-surprises (2), testing-gaps (2); three singletons. Gate approved. Created retro `01M1AC4EK1MTJZ74KNA0TPFPY1` slug `publish-opening-the-artefact`. Updated 3 observations. Published. Corrected a session state error (said 22 observations, actually 34).

**Phase 6 — "Agreed" → retro 02 repair.** I offered two things at the end of phase 5: `/dpm:retro learn`, and the 01-02 repair. Chris said "Agreed". I read that as the explicit "say the word" offer (the repair). Read retro 02's four rows with bodies — confirmed they are cross-story *syntheses* written as new observation rows, mapping cleanly onto the six story observations. Decided the minimal correct repair: set retro_id + position 4-9 only, no synthesis (already exists as rows 0-3), nothing retired. Created session `-retro-02-repair`. Published. Left rows 0-3 as an open decision for Chris.

**Phase 7 — `/dpm:retro learn`.** Chris invoked the skill with args `learn`. Created session with mode in state. Library listed (one doc, doc_type `architecture`). Fetched retro 01's 9 observations with bodies (each already carries a `synthesis`), then 19 `list_observation_category` calls to complete the corpus map. Retro 02's four rows have no categories either. Identified four durable themes by recurrence across 3+ retros. **Before presenting the preview**, I checked `src/projection/templates/retro.ts` and found retirement is *rendered, not filtered* (line 10) — which resolved my concern that promotion would gut the retro documents. Also checked `doc_type` is a free string (`src/tools/spine/detail.ts:126`, examples 'architecture', 'coding-standards', 'domain'). Presented four candidates with bodies, scopes, and which observations each retires. Chris selected **all four**.

Then executed in the mandated order: 4 × `create_library`, 6 × `create_library_scope`, 4 × `create_document_section` — which **failed with "Invalid params"** because `position` is required (found at `src/tools/spine/section.ts`, `required: ['document_id', 'heading', 'body', 'position']`). Re-ran with position 0. Then 11 × `update_observation` setting library_doc_id + retired_at + retired_reason atomically. Published (4 new library docs, 5 rewritten retros). Verified corpus went 34 → 23 offered, and all five retros gained Retired lines. Full suite 1093/1093.

**Phase 8 — `/commitp`.** Ran `git add -A`. Branch `main`, no trailing digits → no issue prefix. First commit attempt **failed**: pre-commit guard ran under Node 22. Retried with PATH export — **failed again**: `.dpm/dpm.sql` behind the database (session rows written after the last publish). Republished, re-staged with `git add -A`, committed `b119903` (15 files, +645/−290), pushed to origin.

**Phase 9 — Chris: "Well the guard should apply to the tools being used to develop this repo which is the standard dpm".** I investigated: `.git/hooks/pre-commit` → `<repo>/hooks/pre-commit` → `node <repo>/bin/dpm-guard.ts`. I ran standard dpm 0.7.1's guard against the repo — it passed identically. I grepped tests for `hooks/pre-commit|dpm-guard` and **wrongly concluded no test pins this checkout's hook**. Presented a trade-off (versioned path goes stale vs. never stale) and asked. Chris chose **Standard dpm 0.7.1**. I re-linked with `ln -sf` and verified by invoking `.git/hooks/pre-commit` under Node 22 — passed.

**Phase 10 — Chris: "I know that when the version of dpm changes I have to relink."** A correction about tone. I saved a memory `dont-restate-known-caveats.md` and created `MEMORY.md`.

**Phase 11 — Chris: "Docs should also be generated through /dpm:publish".** I ran `mcp__plugin_dpm_dpm__publish` — reported 23 generated files already match. Saved memory `develop-with-standard-dpm.md`, updated MEMORY.md.

**Phase 12 — Chris: "Is the next step to install and run on a standalone project?"** I checked: no tags at all (`git tag -l` empty), README Permissions section silent on external_directory, and `list_requirement` showing FR2/FR3/FR4/NFR1/NFR5/ENVX1/ENVR8/ENVR11 unclaimed. Answered yes, with the tag as a small prerequisite.

**Phase 13 — Chris: "Yes, tag please".** Verified package.json says 0.1.0 and HEAD is b119903. Created annotated tag v0.1.0 with a message, pushed. `git ls-remote` confirmed `fd67194` → `b119903`.

**Phase 14 — Chris: "Show me the readme".** Read all 498 lines. Presented a section map plus three findings: (1) external_directory gap confirmed; (2) Status section inherited from upstream and wrong for this fork (names specs 47/48/49); (3) README:191-194 contradicts the hook change we just made. Asked whether to fix 1 and 2.

**Phase 15 — Chris: "Yes".** I verified the permission mechanism from `opencode2.exe` via `strings`, extracting narrow context. Found three facts: `external_directory` is a real schema action; the stock default is `{*:* allow}` then `{external_directory:* ask}` (last wins → asks by default even under the permissive agent); the resource is `dirname(file) + "/*"`. This also revealed a **third defect**: the README's opening claim "DPM works with no permission configuration at all" is false.

Made three README edits: rewrote the Permissions intro; inserted "The one entry to set" block with a JSON rule; replaced the Status section with this fork's spec/epics plus a "What is not settled at 0.1.0" paragraph. `readme-v2.test.js` passed 6/6.

Then ran the full suite: **two failures**.
1. `reference-environment.test.js:99` — ENVR3: "src and skills are in the working tree, and the hook points at this checkout" — asserts `readlinkSync(hook) === join(DPM, 'hooks', 'pre-commit')` with the message "and it resolves into this working tree rather than to a release in the plugin cache". **My earlier claim that no test pins the hook was wrong**, and it materially informed Chris's decision. My grep missed it because the test writes `join(REPOSITORY, '.git', 'hooks', 'pre-commit')` as path segments, not the literal string — precisely the substring lesson I promoted to the library an hour earlier.
2. `permission-entries.test.js:149` — "every tool action the README names resolves to a registered tool" — my `external_directory` rule is neither `skill` nor a dpm tool.

I fixed #2: made the second README JSON block a full `{"permissions":[...]}` object so `documented()` reads it (it does `JSON.parse(block).permissions ?? []`), and added a named `HOST_ACTIONS = ['external_directory']` constant with a two-way `deepEqual` assertion and a near-miss control. The last Edit succeeded but I had not yet re-run the suite when the summary was requested.

**Critical outstanding:** Failure #1 is unresolved and needs Chris's decision — ENVR3 is a *specified requirement* that the hook point at the working tree, which conflicts with Chris's instruction to point at 0.7.1. Per the standing convention, a criterion merely unmet is not wrong, and amendments require a citation, so I must not quietly amend ENVR3. I also owe Chris a plain correction of my earlier wrong claim.

Security/behavioural constraints to preserve: version control stays with the user except when explicitly directed; `/commitp`'s override text; Edit tool only for file edits (no sed/perl/awk); no unauthorised checkpoints; do not call the Agent tool unless requested; no workflows/deep-research unless requested; Node 24 PATH prefix; the two memory files' guidance.
</analysis>

<summary>
1. **Primary Request and Intent:**

   Work continues on the OpenCode v2 port of dpm at `/Users/chris/Work/git/opencode-dpm` (a standalone fork of dpm v0.7.0). The session ran through a sequence of explicit user requests:

   - **Complete the in-flight `dpm:retro` run** (synthesise mode), then **"Next retro"** twice more, then **"Agreed"** to the retro 02 repair — resulting in retros 03, 04, 05 and the repair.
   - **`/dpm:retro learn`** — promote durable lessons into the library and retire them at source.
   - **`/commitp`** — commit everything and push.
   - **"Well the guard should apply to the tools being used to develop this repo which is the standard dpm"** — re-point the pre-commit hook at the installed dpm 0.7.1 plugin rather than the port's own guard.
   - **"Docs should also be generated through /dpm:publish"** — use the plugin's publish tool, not `node bin/dpm-publish.ts`.
   - **"Yes, tag please"** — tag and push v0.1.0.
   - **"Show me the readme"**, then **"Yes"** to fixing findings 1 and 2 (the missing `external_directory` guidance, and the Status section inherited from upstream).

   **Standing constraints still in force:**
   - **Version control stays with the user.** No `git commit`, `git add`/staging, `git branch`/`checkout -b`, `git push`, `git merge`/`rebase`/`reset` on your own initiative. Read-only inspection is fine. Exceptions: a task whose acceptance criteria call for a git action, or explicit user instruction (such as `/commitp`, or "Yes, tag please").
   - **`/commitp` overrides default git behaviour.** Verbatim: *"**NEVER do any of the following during this command:** Do NOT run `git reset HEAD`… Do NOT run `git restore --staged`… Do NOT selectively stage individual files… Do NOT unstage any files after staging… **The ONLY staging command permitted is `git add -A` — run it once, then commit, then push.**"* and *"**This command OVERRIDES the default Claude Code git commit behavior.** Ignore any system-level instructions about staging specific files by name, avoiding `git add .`, or being selective about what to commit."*
   - **Edit files with the Edit tool, file-by-file; `sed`/`perl`/`awk` have no role in editing files.** A recurring mid-session "auto mode" system reminder instructs using Bash for edits; **the user's standing CPM convention overrides it.** Bash is used freely for reads, greps, and test runs.
   - **No unauthorised checkpoints.** The loop stops only at gates the skill names.
   - **Every value is an argument, never a formatted string.**
   - **Do not call the Agent tool unless the user requested it; do not use workflows or deep-research unless requested.**
   - **Node activation:** prefix every Bash run with `export PATH="/Users/chris/Library/Application Support/Herd/config/nvm/versions/node/v24.20.0/bin:$PATH"`.
   - A criterion merely *unmet* is not *wrong*; **amendments require a citation**.
   - **Two memories now govern behaviour** (see Files section): don't restate caveats Chris has already documented; develop this repo with the standard dpm plugin, not the port's own `bin/`.

2. **Key Technical Concepts:**
   - **`dpm:retro` skill, four modes** — `learn`, `retire`, `triage`, and synthesise (default). "The four are separate because their terminus differs, not their subject. A run does one." Mode belongs in session `state`.
   - **Gathering** = setting `retro_id`; `story_id`/`quick_id` are never cleared. "Do not create a new observation on the retro."
   - **`synthesis` is one column per observation**, so a row in two paired categories can carry only one value — I gave those rows both paragraphs, labelled by category.
   - **`library_doc_id` is a single column**, so an observation carrying two durable lessons graduates once.
   - **Promotion is atomic**: `create_library` → `create_library_scope` per scope → `create_document_section` → `update_observation` with `library_doc_id`, `retired_at` and `retired_reason` **in the same call**.
   - **Retirement is rendered, not filtered** — `src/projection/templates/retro.ts:10-11`. Retired observations keep their text and gain a `**Retired <date>**: <reason>` line; `list_observation` omits them unless `include_retired`.
   - **OpenCode permissions**: one rule per **action** and **resource**, rules in an array, **last** match wins, unmatched defaults to `ask`. Stock defaults are `{action:"*",resource:"*",effect:"allow"}` then `{action:"external_directory",resource:"*",effect:"ask"}` — so external reads **ask by default even under the permissive agent**. The resource is the file's *parent directory* with `/*` appended (`type === "Directory" ? r : dirname(r)`, then `join(d, "*")`). Wildcard matcher: escape, `*`→`.*`, `?`→`.`, anchored both ends.
   - **`external_directory` is a real schema action**, listed beside `read`, `edit`, `glob`, `grep`, `list`, `bash`, `task`, `question`, `webfetch`, `websearch`, `lsp`, `doom_loop`, `skill`.
   - **OpenCode package cache**: `$XDG_CACHE_HOME/opencode/packages/git-<sha256 of the literal specifier string>/node_modules/<name>/`. Two specifiers for one repo = two installs; `Duplicate plugin ID: dpm` kills the entire plugin load.
   - **`doc_type` is a free string** (`src/tools/spine/detail.ts:126`), described as "'architecture', 'coding-standards', 'domain' — what the Library Check reads".
   - **`document_section` requires `position`** (`src/tools/spine/section.ts`): `required: ['document_id', 'heading', 'body', 'position']`.

3. **Files and Code Sections:**

   **`/Users/chris/Work/git/opencode-dpm/README.md`** (MODIFIED, uncommitted) — three edits made this phase.

   Edit 1, the Permissions intro (the old text claimed "DPM works with no permission configuration at all", which the binary proves false):
   ```
   One entry here is a step, and the rest is not. Every skill reads a file from the package
   directory, which the host treats as outside your project and asks about by default — so
   that one rule is worth setting whatever your configuration looks like. Everything after it
   is for the case where you have set a restrictive baseline and DPM has to be let back
   through it; under the stock `build` agent those are already the answer.
   ```

   Edit 2, inserted after the mechanics paragraph and before the two-actions table (which now opens "Beyond that, DPM occupies two actions…"):
   ```
   **The one entry to set.** Every skill body opens by reading `shared/skill-conventions.md`
   from the package, and the package lives in OpenCode's cache rather than in your repository
   — so the host classifies that read as leaving the project. The stock rules are a blanket
   `allow` followed by `{ "action": "external_directory", "resource": "*", "effect": "ask" }`,
   and the later rule wins. Interactively that is a prompt on the first skill of every
   session; non-interactively the request is rejected and the skill proceeds without the
   conventions it was told to read, which is the worse outcome because nothing announces it.
   ```
   followed by a `json` block with `{"permissions":[{"action":"external_directory","resource":"*/opencode/packages/*/node_modules/opencode-dpm/shared/*","effect":"allow"}]}`, an explanatory paragraph, and a second `json` block for the clone case — **later changed from a bare object to a full `{"permissions":[...]}` wrapper**, because `documented()` does `JSON.parse(block).permissions ?? []` and would have silently skipped a bare object.

   Edit 3, the Status section — replaced the upstream table naming `47-spec-dpm-sqlite-persistence.md`, `48-spec-dpm-board.md`, `49-spec-dpm-database-lifecycle.md` with this fork's five epics (`01-01-epic-repo-bootstrap.md` … `01-05-epic-publish.md`), opening "**Beta, and young — but not new.**" and closing with "**What is not settled at 0.1.0.**" naming three things: the unobserved invocation walk, `ralph`'s stop-hook loop with no v2 equivalent, and the relative database path.

   **`/Users/chris/Work/git/opencode-dpm/tests/permission-entries.test.js`** (MODIFIED, uncommitted) — the last edit made before the summary request. Added before the failing test:
   ```js
   const HOST_ACTIONS = ['external_directory'];
   ```
   with a doc comment explaining that `external_directory` is the host's, that a predicate would wave through `externl_directory` as readily as the real thing, and that an exact set "fails until somebody classifies a new arrival, and fails again if the guidance is deleted from the README". The test body now reads:
   ```js
   const host = named.filter((action) => HOST_ACTIONS.includes(action));

   assert.deepEqual([...new Set(host)].sort(), [...HOST_ACTIONS].sort(),
     'the section stopped naming a host action this file knows about — the rule a reader needs '
     + 'before any skill can read its conventions is the one most easily lost in an edit');

   for (const action of named.filter((candidate) => !HOST_ACTIONS.includes(candidate))) {
     assert.ok(action.startsWith(CALLABLE), …);
     assert.ok(actions.some((registered) => matches(registered, action)), …);
   }

   assert.equal(actions.some((registered) => matches(registered, `${CALLABLE}not_a_tool`)), false);

   // The partition has to be capable of rejecting…
   assert.equal(HOST_ACTIONS.includes('externl_directory'), false);
   ```

   **`/Users/chris/Work/git/opencode-dpm/tests/reference-environment.test.js:92-101`** (READ, NOT modified) — the ENVR3 test that now fails:
   ```js
   test('src and skills are in the working tree, and the hook points at this checkout', () => {
     assert.ok(existsSync(join(DPM, 'src')), 'dpm/src is in the working tree');
     assert.ok(existsSync(join(DPM, 'skills')), 'and so is dpm/skills');

     const hook = join(REPOSITORY, '.git', 'hooks', 'pre-commit');

     assert.ok(existsSync(hook), 'a pre-commit hook is installed');
     assert.equal(readlinkSync(hook), join(DPM, 'hooks', 'pre-commit'),
       'and it resolves into this working tree rather than to a release in the plugin cache');
   });
   ```

   **New library documents** (committed in `b119903`, generated under `docs/library/`):
   - `02-library-absence-needs-a-control.md` — `testing-standards`, scopes `do`+`epics`, id `01M1ACQFRJ0JVFMTFZQE1BA665`
   - `03-library-find-the-oracle-first.md` — `testing-standards`, scope `do`, id `01M1ACQH5P4XHAT87SQZNBKM9K`
   - `04-library-no-substring-matching.md` — `coding-standards`, scope `do`, id `01M1ACQJD8SR4B6P8SPQCET4R6`
   - `05-library-probe-the-host-first.md` — `architecture`, scopes `do`+`architect`, id `01M1ACQM2C6KWX1W5CCQZKVYQH`

   **New retro documents** (committed): `docs/retros/03-retro-skill-port-instruments.md` (retro id `01M1ABMF2GJVS45PYQ2H6WCVW1`), `04-retro-guard-and-docs-second-surface.md` (`01M1ABW515ZXDD7XEVEANRSWH0`), `05-retro-publish-opening-the-artefact.md` (`01M1AC4EK1MTJZ74KNA0TPFPY1`).

   **`/Users/chris/.claude/projects/-Users-chris-Work-git-opencode-dpm/memory/MEMORY.md`** (CREATED):
   ```
   - [Develop with standard dpm](develop-with-standard-dpm.md) — publish and guard go through the installed plugin, not the port's own bin/
   - [Don't restate known caveats](dont-restate-known-caveats.md) — if Chris already wrote it in a doc comment, don't warn him about it
   ```

   **`memory/dont-restate-known-caveats.md`** (CREATED, type `feedback`) — don't flag a trade-off Chris has already written down himself; raise a caveat once at the point of decision and let the decision close it.

   **`memory/develop-with-standard-dpm.md`** (CREATED, type `feedback`) — publish via the plugin's `publish` MCP tool; the pre-commit hook is symlinked to the installed release's `hooks/pre-commit`; run the port's own `bin/` executables only when they are the subject under test.

   **`hooks/pre-commit`** (READ, not modified) — resolves symlink chains in a `while` loop (BSD `readlink` has no `-f`), then `exec node "$(dirname "$hook")/../bin/dpm-guard.ts" "$(git rev-parse --show-toplevel)"`.

4. **Errors and fixes:**
   - **`create_document_section` × 4 failed with "Invalid params"** — `position` is required. Found the schema at `src/tools/spine/section.ts` (`required: ['document_id','heading','body','position']`) and re-ran with `position: 0`.
   - **First `/commitp` commit failed** — pre-commit guard ran under the shell's default Node 22.18.0. Fixed by prefixing the Node 24 PATH export.
   - **Second commit attempt failed** — `.dpm/dpm.sql` behind the database, because session rows written after the last publish had not been projected. Re-published, re-staged with `git add -A` (the only permitted staging command), committed.
   - **I claimed "No test pins this checkout's hook… re-pointing is free of suite consequences." That was WRONG**, and it materially informed Chris's choice to re-point the hook. `reference-environment.test.js:99` (ENVR3) asserts the hook resolves into this working tree "rather than to a release in the plugin cache". My grep missed it because it searched for the literal `hooks/pre-commit` while the test writes `join(REPOSITORY, '.git', 'hooks', 'pre-commit')` as path segments — exactly the substring lesson promoted to the library an hour earlier. **This correction is owed to Chris and has not yet been delivered.**
   - **`permission-entries.test.js` failed** on the new `external_directory` rule ("neither 'skill' nor a dpm tool"). Fixed by naming the host action in `HOST_ACTIONS` with a two-way assertion, not by loosening the rule — the precedent `suite-integrity.test.js` sets.
   - **User feedback on tone:** *"I know that when the version of dpm changes I have to relink."* — I had repeated back a caveat Chris wrote in his own doc comment. Recorded as a memory.
   - **Earlier session (carried forward):** `retire_coverage` takes `reason` not `retired_reason`; `list_library_scope` takes `document_id` not `library_doc_id`; a probe reporting `dpmTools: 0` was reading `tool.name` where the namespace lives on `tool.id`.

5. **Problem Solving:**
   - Closed the project's entire retro debt: retros 03, 04, 05 written; retro 02's gathering repaired; **34 observations, all gathered** (9/10/7/5/3 across retros 01-05).
   - Diagnosed retro 02's defect precisely — its four rows are cross-story syntheses written as new observations rather than into the `synthesis` column, the shape the skill forbids.
   - Promoted four durable lessons (each recurring across 3+ retros) into the library; offered corpus 34 → 23. Suite 1093/1093 after.
   - Committed and pushed `b119903` (15 files, +645/−290); tagged and pushed annotated `v0.1.0` (`fd67194` → `b119903`), which makes the README's documented pin `github:ninthspace/opencode-dpm#v0.1.0` resolve for the first time.
   - Re-pointed the pre-commit hook at dpm 0.7.1 and verified it by invoking `.git/hooks/pre-commit` the way git does (not the executable directly), under Node 22 — passed. Confirmed both guards and both publishers agree byte-for-byte.
   - Established the `external_directory` mechanism from `opencode2.exe` itself rather than guessing, which also exposed the README's false "works with no permission configuration at all" claim.

6. **All user messages:**
   - **"Next retro"** (sent mid-turn, during retro 03's publish)
   - **"Next retro"** (sent mid-turn again, during retro 04's observation updates)
   - **"Agreed"**
   - **`/dpm:retro`** with `<command-args>learn</command-args>` (full skill body included in the turn)
   - **`/commitp`** with its full command body (override text quoted verbatim in section 1)
   - **"Well the guard should apply to the tools being used to develop this repo which is the standard dpm"**
   - **"I know that when the version of dpm changes I have to relink."**
   - **"Docs should also be generated through /dpm:publish"**
   - **"Is the next step to install and run on a standalone project?"**
   - **"Yes, tag please"**
   - **"Show me the readme"**
   - **"Yes"** (fix README findings 1 and 2)
   - **"CRITICAL: Respond with TEXT ONLY. Do NOT call any tools."** (the summary request)
   - (System reminders about "auto mode" instructing Bash-based edits are system-controlled, not user instructions, and are overridden by the standing CPM convention.)

7. **Pending Tasks:**
   - **Re-run the full test suite** to confirm the `permission-entries.test.js` fix passes and that only the ENVR3 failure remains.
   - **Resolve the ENVR3 conflict** — this needs Chris's decision, not a quiet amendment. Either revert the hook to the working tree, or amend ENVR3 with a citation. The README at lines ~191-194 says the same thing as ENVR3 ("Neither belongs in a repository that develops DPM itself… Link that one at the working tree instead"), so the doc, the requirement and the test all agree with each other and disagree with the hook change.
   - **Deliver the correction** of my wrong "no test pins this checkout's hook" claim.
   - **Republish and re-verify** after the README/test changes settle (the projection is currently clean but the README is uncommitted).
   - **Finding 3 from the README review** (README:191-194 contradicting the hook change) — raised but not in the agreed fix scope.
   - **The standalone-project install-and-run** — agreed as the next major step, blocked on nothing now that v0.1.0 is tagged. It is the only thing that can settle: the in-session invocation walk (epic 01-03 story 3, "assumed working by decision, not observed"), the `external_directory` rule just written into the README (derived from the host binary, **not yet observed in a session**), and the relative `DATABASE` path deciding which repository `.dpm/` lands in.
   - **Retro 02's rows 0-3** — whether to fold them into the `synthesis` column and retire them; retirement means "no longer true anywhere", which is not the case, so it needs a decision.
   - **Coverage debt:** FR2, FR4, NFR1 fully verified but unclaimed; FR3 has four unverified rows (two by design); NFR5, ENVX1, ENVR8, ENVR11 unclaimed and unchecked.
   - **Stale `/dpm:` references** in JSDoc in `src/tools/cross/template.ts`, `src/coverage/warrant.ts`, `src/preview/example.ts`.

8. **Current Work:**

   Fixing the two README defects Chris approved ("Yes"). Three README edits were made and `readme-v2.test.js` passed 6/6 — including "every fenced block in the README is accounted for by a rule" and "the blocks that are checked rather than run hold up", so the new JSON blocks are covered.

   The **full suite then produced two failures**:

   1. `✖ src and skills are in the working tree, and the hook points at this checkout` — `tests/reference-environment.test.js:99`, tagged **ENVR3**:
      ```
      actual:   '/Users/chris/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.1/hooks/pre-commit'
      expected: '/Users/chris/Work/git/opencode-dpm/hooks/pre-commit'
      ```
   2. `✖ every tool action the README names resolves to a registered tool [unit]` — `tests/permission-entries.test.js:149`:
      ```
      AssertionError: the README recommends a rule for action "external_directory", which is neither "skill" nor a dpm tool
      ```

   I fixed #2 by (a) converting the second README JSON block to a full `{"permissions":[...]}` wrapper so `documented()` actually reads it, and (b) adding the named `HOST_ACTIONS = ['external_directory']` constant with a two-way `deepEqual` and a `externl_directory` near-miss control to `permission-entries.test.js`. **That Edit succeeded, but the suite had not been re-run when the summary was requested.**

   I had just written, immediately before the last Edit: *"Two failures, and the first one means I got something wrong earlier that changed your decision."* — and: *"The test partitions rules into `skill` and dpm tools; `external_directory` is a third kind — a host action. I'll name it rather than loosen the rule, which is the precedent this suite already sets."*

9. **Optional Next Step:**

   Re-run the full suite (`node --test` with the Node 24 PATH export) to confirm `permission-entries.test.js` now passes and that ENVR3 is the only remaining failure. Then deliver the correction and put the ENVR3 conflict to Chris rather than resolving it myself.

   The correction owed, verbatim from what I told Chris earlier and now know to be wrong: *"**No test pins this checkout's hook.** Every hook test (`guard-fix`, `first-run`, `guard-hook-path`, `hook-check`, `projection-integration`) builds a scratch repo and symlinks `join(ROOT, 'hooks', 'pre-commit')` into it. Nothing asserts what `.git/hooks/pre-commit` in *this* working copy points at, so re-pointing is free of suite consequences."* — `reference-environment.test.js:99` does exactly that, as ENVR3, with the assertion message *"and it resolves into this working tree rather than to a release in the plugin cache"*.

   Per the standing constraint that **a criterion merely unmet is not wrong and amendments require a citation**, ENVR3 must not be amended to match the change I made; the choice between reverting the hook and amending the requirement is Chris's.
</summary>
