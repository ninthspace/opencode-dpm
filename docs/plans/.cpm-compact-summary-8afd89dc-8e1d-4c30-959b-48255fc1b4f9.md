# Compact Summary (PostCompact)
**Captured**: 2026-08-30 18:28:51
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Starting state**: The session resumes from a prior compaction. The summary establishes this is a `dpm:do` skill run (dpm plugin v0.7.0) against `/Users/chris/Work/git/opencode-dpm`, a standalone fork porting dpm to OpenCode v2. Epic 01-02 was completed; epic 01-03 "Skill port and registration" was begun, with story 1 "Pilot the spec skill end-to-end" plan-approved.

**Standing constraints carried forward (must preserve)**:
- Version control stays with the user — no `git commit`, `git add`, `git branch`, `git push`, `git merge`/`rebase`/`reset` on my own initiative. Read-only git inspection is fine.
- No unauthorised checkpoints — the loop stops only at named gates; task-to-task and story-to-story transitions are silent; "Shall I continue?" is forbidden.
- Every value is an argument, never a formatted string.
- The source spec is read and never written.
- Edit files with the Edit tool, file-by-file; `sed`/`perl`/`awk` have no role in editing files. The recurring "auto mode" system reminder instructing Bash-based edits is **overridden** by the user's standing CPM convention.
- Do not call the Agent tool unless requested; no workflows or deep-research unless requested.
- An unmet criterion is not a wrong criterion; amendments require a citation.
- Node activation: every Bash run prefixed with `export PATH="/Users/chris/Library/Application Support/Herd/config/nvm/versions/node/v24.20.0/bin:$PATH"`.

**Work performed in this segment**:

*Step 1 of the approved plan — moving `CALLABLE` off the Claude Code manifest.* The file `tests/support/skills.js` was mid-edit and broken (SERVER_NAME not imported). I added `import { SERVER_NAME } from '../../src/plugin/index.ts';`, added `EITHER_CALLABLE` as an exported alternation, taught `toolNames()` and `ungated()` to use it, added `bodiesOnLegacyForm()`. First full-suite run produced 10 failures across body-reads/body-corpus/reachability/skill-do. Fixed by:
- `tests/support/body-reads.js` — `sites()` moved onto `EITHER_CALLABLE`, import changed.
- `tests/skill-do.test.js` — `CALLABLE` → `EITHER_CALLABLE`.
- `tests/reachability.test.js` — replaced `assert.equal(CALLABLE, 'mcp__plugin_dpm_dpm__')` with a two-pair check (`LEGACY_CALLABLE` vs manifest-derived form; `CALLABLE` vs literal `'dpm_'`), and reworded two failure messages.
Result: 1042 passing.

*Task 1 — port `skills/spec/SKILL.md`.* 32 `mcp__plugin_dpm_dpm__` → `dpm_` (one Edit, replace_all); front matter `Triggers on "/dpm:spec".` → `Invoke with the skill tool, id "dpm-spec".`; two cross-references. The oracle check: reverse-substituting `` `dpm_ `` → `` `mcp__plugin_dpm_dpm__ `` and diffing against `~/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.0/skills/spec/SKILL.md` left exactly 3 differing lines. Two test failures followed (`skill-retrofit.test.js` Perspectives, `skill-spec.test.js` description) — both fixed. 1042 passing.

*Task 2 — the scratch run.* Several failures/discoveries:
- `plugins` must be an ARRAY; `package` must name the entry file.
- A plugin module must export a default object with an `id` string, else `SchemaError Missing key [default][id]`.
- `/api/skill` returns `[]` for plugin-registered skills — the probe (`ctx.skill.transform(draft => draft.list())`) is the only reader.
- `opencode2 run` with `mtplx/mtplx` → `ConnectionRefused`. With `opencode/nemotron-3.5-lightning-free`: the skill loaded (`→ Skill "spec"`) then hit `permission requested: external_directory (/Users/chris/Work/git/opencode-dpm/shared/*); auto-rejecting` — a major finding.
- Added `permission: { external_directory: "allow", read: "allow" }`; re-ran with `nemotron-3-ultra-free` in the background; the model shelled into the real repo running `sqlite3 .dpm/dpm.db` SELECTs. I killed it via TaskStop and recorded "do not hand an autonomous model bash again."
- Probe evidence: 55 skills / 23 dpm's; 195 tools / 183 `dpm_`-prefixed; all 24 of spec's tools present, `missing: []`; conventions substituted to an absolute path that exists.
- `draft.get('dpm_create_spec').execute(...)` first failed "Unable to execute" with a stub sessionID; fixed by creating a real session via `ctx.session.create({ body: { title: ... } })`. Row written, verified it landed in the scratch DB not the real one.
- Scratch project deleted.

*Task 3 — the rewrite-pattern section* recorded on epic 01-03 via `create_document_section` (position 0), covering the four edit classes with counts, the corrected `slash` premise, what the pilot proved, the external_directory finding, ralph's exception, and the test-side transition.

*Task 4 — `tests/skill-pilot.test.js`* created with 5 tests. Learned that `tests/vendoring.test.js` forbids any source naming `ninthspace-marketplace` (outside comments), so the oracle diff stays manual. Registered in `suite-integrity.test.js` ADDED list. 1047 passing, tsc clean, module sweep clean.

*Verification gate.* Criterion 1's "gates" limb unmet. I asked Chris via AskUserQuestion; **Chris chose "Complete anyway, record the gap"**. I marked coverage `01M194YRXJ2T0WZT4EPWDWQ2YT` verified and left `01M194YPV1N00588YDCT47GW1Q` unverified. Refactoring pass extracted `toolMentions()`. Story closed, two observations recorded.

*Peer messages.* Two cross-session messages from `claude-code-marketplace-0c` about upstream dpm 0.7.2 narration rules and a database rollback in their repo. I deferred the narration port (it would destroy the v0.7.0 diff check) and replied twice.

*Story 2 begun* — "Port and register all twenty-three skills", plans inline. Task 1: all three edit classes applied across the remaining 22 skills + `shared/`. Reverse-diff against the oracle: 0 differing lines for all 22 and `shared/skill-conventions.md`, 6 for `spec`. Then the transition machinery was retired (`LEGACY_CALLABLE`, `EITHER_CALLABLE`, `bodiesOnLegacyForm` deleted). 33 test failures followed from hard-coded legacy prefixes across ~23 test files; all fixed. Then 4 more (status table `/dpm:` patterns, skill-epics description, skill-pilot `LEGACY_CALLABLE` ReferenceError). Now 1045 passing.

*Task 3 of story 2* verified in a fresh scratch project: 23 dpm skills, all `dpm-` prefixed, locations inside the package, MCP connected, 195/183 tools, **zero unresolved tool names**, zero legacy, zero `/dpm:`, 24 shared refs all absolute+existing, 23 descriptions naming the skill tool, 5 `.claude/` in `dpm-ralph` only.

**Immediately before the summary request**: I had just sent the second reply to the peer session and was about to put the `ralph` question to Chris — criterion 5 (must_not: a skill body names `mcp__plugin_`, `/dpm:`, `CLAUDE_PLUGIN_ROOT`, or `.claude/`) is violated only by `ralph`, which names a Claude Code stop hook 29 times and whose hook was never shipped even in v0.7.0.

**User messages**: Only two actual user-role turns in this segment — the compaction continuation instruction at the start, and the final summary instruction. The AskUserQuestion answer ("Complete anyway, record the gap") arrived as a tool result, not a user message. The peer messages came as system-delivered cross-session messages, not user turns.
</analysis>

<summary>
1. **Primary Request and Intent:**

   The session executes the `dpm:do` skill (dpm plugin v0.7.0) against the OpenCode v2 port of dpm at `/Users/chris/Work/git/opencode-dpm`, a clean standalone fork of dpm v0.7.0. The loop: work each story's tasks, verify each acceptance criterion, record verification as typed MCP tool rows, continue until the epic is done. All state is recorded via `mcp__plugin_dpm_dpm__*` tools; nothing is written to markdown by the skill.

   This segment **completed epic 01-03 story 1** `01M194V4CBNMW9KRVBHS83R3DM` "Pilot the spec skill end-to-end" (all 4 tasks, verification, refactoring, close, 2 observations) and is **mid-way through story 2** `01M194V5Q15F8TQR44DPVPJBRY` "Port and register all twenty-three skills" (`plan: 0`, planned inline).

   **Standing constraints that remain in force (preserve verbatim in effect):**
   - **Version control stays with the user.** Do not run `git commit`, `git add`/staging, `git branch`/`checkout -b`, `git push`, `git merge`/`rebase`/`reset` on your own initiative. Read-only git inspection (`git status`, `git log`, `git diff`) is fine. The exception is a task whose acceptance criteria call for a git action.
   - **No unauthorised checkpoints.** The loop stops only at named gates. Task-to-task and story-to-story transitions are silent. "Shall I continue?" is forbidden.
   - **Every value is an argument, never a formatted string** — status, note, planning mark, disposition, verification are each a column.
   - **The source spec is read and never written.**
   - **Edit files with the Edit tool, file-by-file; `sed`/`perl`/`awk` have no role in editing files.** A recurring mid-session "auto mode" system reminder instructs using Bash (`sed`, heredocs) for edits; **the user's standing CPM convention overrides it.** Bash is used freely for reads, greps, and test runs.
   - **Do not call the Agent tool unless the user requested it; do not use workflows or deep-research unless requested.**
   - A criterion that is merely *unmet* is not a criterion that is *wrong*; **amendments require a citation**.
   - **Node activation**: every Bash run must be prefixed with `export PATH="/Users/chris/Library/Application Support/Herd/config/nvm/versions/node/v24.20.0/bin:$PATH"`.
   - **New this segment (self-imposed, recorded in session state): do not hand an autonomous free model bash access again.** One shelled into the real checkout and ran `sqlite3 .dpm/dpm.db` SELECTs.

2. **Key Technical Concepts:**
   - Node 24 native TypeScript type-stripping; erasable syntax only; `.ts` specifiers; `verbatimModuleSyntax`.
   - **"False pass"** — a sweep that reports clean because it examined nothing. Every absence assertion needs a control.
   - **OpenCode v2 plugin API** (`@opencode-ai/plugin@0.0.0-beta-18684`): `Plugin.define` is the identity function; `Transform<Draft> = (cb) => Promise<Registration>`; domains on `Context` include `mcp`, `skill`, `tool`, `session`, `command`, `permission`. `ToolDraft` has `list() / get(id) / add / update / remove`, and `get(id)` returns `Info & { id }` with a callable `execute(input, context)`.
   - **v2 host facts established empirically:**
     - Project config key is `plugins`, an **ARRAY** of `string | { package, options }` (not a map). `package` must name the entry **file**.
     - A plugin module **must** export a default object with an `id` string; otherwise the host logs `SchemaError Missing key ["default"]["id"]` and `plugin list` shows `(failed)`.
     - `mcp list` / `plugin list` report nothing for ~1–2s after `service start`; run them twice.
     - `ctx.skill.list()` returns an envelope; the reader is `ctx.skill.transform(draft => draft.list())`. The HTTP `/api/skill` returns `[]` for plugin-registered skills — the probe plugin is the only reader.
     - `draft.get('dpm_x').execute(input, ctx)` works only with a **real** `sessionID` from `ctx.session.create(...)`; a stub id gives `"Unable to execute dpm_create_spec"`.
     - Registered surface: **195 tools = 183 `dpm_`-prefixed + 12 built-ins; 55 skills of which 23 are dpm's.**
     - **`Skill.Info.slash` is catalogue visibility (`false` hides; unset = visible). It mints NO `/name` trigger.** The documented invocation is the model calling the built-in `skill` tool with the **exact, case-sensitive ID** (e.g. `dpm-spec`). Source: `https://opencode.ai/v2/docs/skills`. Story 3's criterion therefore needs **no** amendment.
     - `permission` config accepts `ask | allow | deny` or a glob record, on keys including `read`, `edit`, `glob`, `grep`, `list`, `bash`, `task`, `external_directory`, `question`, `webfetch`, `websearch`, `lsp`, `doom_loop`.
   - **`update_story`'s `status_note` has a short length cap** (a long note returns `Invalid params`); task `status_note` does not.
   - `create_observation` `position` is unique per retro/story; the second and later rows must set it.
   - The oracle: `~/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.0/`. `tests/vendoring.test.js` forbids any source (outside comments, via `withoutComments`) from naming `ninthspace-marketplace`, so oracle diffs stay manual checks.

3. **Files and Code Sections:**

   **`tests/support/skills.js`** — the hub read by 40 test files. Final state of the prefix machinery (after story 2's retirement of the transition):
   ```js
   import { SERVER_NAME } from '../../src/plugin/index.ts';
   ...
   export const CALLABLE = `${SERVER_NAME.replaceAll(/[^A-Za-z0-9_-]/g, '_')}_`;

   /** ... **This read both prefixes while the port was in flight, and no longer does.** ... */
   export const toolMentions = (source) => source.matchAll(new RegExp(`${CALLABLE}([a-z_]+)`, 'g'));

   export function toolNames(source) {
     const found = [...toolMentions(source)].map((hit) => hit[1]);
     return [...new Set(found)].sort();
   }
   ```
   `ungated()` uses `new RegExp(`${CALLABLE}(create|update)_`)`. `LEGACY_CALLABLE`, `EITHER_CALLABLE` and `bodiesOnLegacyForm()` existed transiently and were **deleted** once story 2 emptied the legacy list.

   **`tests/support/body-reads.js`** — `sites()` now calls `toolMentions(source)`; import is `import { conventions, prose, skillNames, skillSource, toolMentions } from './skills.js';`.

   **`tests/support/vocabulary.js`** — added `import { CALLABLE } from './skills.js';` and changed the taxonomy reader to `if (!new RegExp(`${CALLABLE}list_taxonomy`).test(body))`.

   **`skills/spec/SKILL.md`** — 32 tool refs → `dpm_`; description now ends `Invoke with the skill tool, id "dpm-spec".`; cross-references became `` the `dpm-epics` skill `` / `` the `dpm-architect` skill ``; the `dpm/shared/skill-conventions.md` line untouched.

   **All 22 remaining `skills/*/SKILL.md` + `shared/skill-conventions.md` + `shared/status-model.md`** — 424 tool refs → `dpm_`; 22 descriptions → `Invoke with the skill tool, id "dpm-<name>".`; 101 `` `/dpm:X` `` → `` `dpm-X` ``. Argument examples reworked: audit's `` `$ARGUMENTS` is an optional scope hint — `auth`, or `src/billing`. ``; quick's `` `$ARGUMENTS` is the change description — `add a --verbose flag to the deploy script`. ``; retro's `` `dpm-retro` with `learn` ``.

   **`tests/skill-pilot.test.js`** (NEW, story 1 task 4) — 3 tests after the story-2 trim: the pattern is on the epic carrying the prefix rule, invocation form, the `slash` correction and the ralph exception; every tool the pilot names resolves against `spineTools`; the pilot names no Claude Code mechanism (driven against 3 planted breaches). Registered in `suite-integrity.test.js`'s `ADDED` as `'skill-pilot.test.js', //            01-03 story 1 — one body ported, and the transition's tripwire`.

   **`tests/tool-naming.test.js`** — the deliberately-transient assertion flipped:
   ```js
   assert.doesNotMatch(conventions, /mcp__plugin_dpm_dpm__/,
     'the shared conventions still name Claude Code\'s prefix');
   assert.match(conventions, new RegExp(rendered(SERVER_NAME, 'list_taxonomy')),
     'and they name no v2 tool either, so the reading above found nothing in either direction');
   ```

   **`tests/reachability.test.js`** — the manifest pairing simplified to `assert.equal(CALLABLE, 'dpm_');` with the Claude Code pairing removed; two failure messages reworded.

   **`tests/reference-handoff.test.js`** — `row.match(/`dpm-([a-z]+)`/)`. **`tests/skill-naming.test.js`** — `rows.filter((row) => /`dpm-/.test(row))`. **`tests/skill-epics.test.js`** — description assertion now checks `/skill tool, id "dpm-epics"/` and `doesNotMatch(/\/dpm:/)`.

   **~20 other test files** moved off hard-coded `mcp__plugin_dpm_dpm__` onto `new RegExp(`${CALLABLE}...`)` with `CALLABLE` added to their `./support/skills.js` imports: skill-clean, skill-library, skill-consult, skill-pivot, skill-inspect, skill-ralph, skill-party, skill-discover, skill-review, skill-retro, skill-quick, skill-retrofit, skill-do. `skill-publish.test.js` moved onto the shared `toolNames(source)`. Fixture strings in `skills-resume.test.js`, `skills-gates.test.js`, `vocabulary.test.js` moved to the `dpm_` form. Stale comments updated in `naming.test.js`, `skill-review.test.js`, `support/body-reads.js`, `support/skills.js`.

   **`src/plugin/index.ts`** (unchanged, read for context) — `export const SERVER_NAME = 'dpm';`, default export `{ id: 'dpm', async setup(context) { ... } } satisfies Plugin.Plugin`, with both registrations computed before either transform runs (ADR 01-07) and disposal in reverse.

4. **Errors and fixes:**
   - **`tests/support/skills.js` broken on resume** — `SERVER_NAME` used but not imported. Fixed by adding the import from `../../src/plugin/index.ts`.
   - **10 test failures after `CALLABLE` changed meaning** — `body-reads.js`'s `sites()` found nothing so every classification read as stale; `skill-do.test.js` built its expectation from `CALLABLE`; `reachability.test.js` asserted `CALLABLE === 'mcp__plugin_dpm_dpm__'`. Fixed by introducing `EITHER_CALLABLE` and moving those readers onto it.
   - **`skill-retrofit.test.js` Perspectives test** hard-coded the legacy prefix in two regex literals — moved to `new RegExp(`\`${EITHER_CALLABLE}list_agent\`...`)`, later to `CALLABLE`.
   - **`skill-spec.test.js:270`** asserted `/\/dpm:spec/` in the description — replaced with a two-sided check (`match(/skill tool, id "dpm-spec"/)` + `doesNotMatch(/\/dpm:/)`).
   - **Probe plugin `(failed)`** — `SchemaError Missing key ["default"]["id"]`. Fixed by adding `id: 'pilot-probe'`.
   - **`opencode2 run --model mtplx/mtplx`** → `ConnectionRefused` (local provider down).
   - **First free-model run blocked** by `permission requested: external_directory (...); auto-rejecting` — this became a recorded **finding**, not just an obstacle.
   - **Second free-model run** (`nemotron-3-ultra-free`, backgrounded) shelled into the real repo running `sqlite3 .dpm/dpm.db`. Killed with TaskStop; confirmed the working tree and the real database were untouched; recorded "do not hand an autonomous model bash again."
   - **`draft.get('dpm_create_spec').execute(...)` → "Unable to execute"** — the stub `sessionID` was invalid. Fixed by `await ctx.session.create({ body: { title: 'pilot probe' } })` and passing its `id`.
   - **33 test failures after the batch rewrite** — ~23 test files carried hard-coded `mcp__plugin_dpm_dpm__` in regex literals and fixtures. All fixed as listed above.
   - **4 residual failures** — `reference-handoff.test.js` and `skill-naming.test.js` matched the status table on `` `/dpm: ``; `skill-epics.test.js` asserted the old description; `skill-pilot.test.js` threw `ReferenceError: LEGACY_CALLABLE is not defined` (the constant was deleted but a literal use remained) — replaced with the literal string `'mcp__plugin_dpm_dpm__'` in that one assertion, with a comment saying it is the port's own history.
   - **My own earlier planning premise about `slash` was wrong** (that v2 mints slash commands for skills). Corrected in the session state and written into the epic section so nobody re-derives it.

5. **Problem Solving:**
   - Made the prefix transition a first-class, self-policing object rather than an implicit dual-form matcher, then retired it in one pass the moment story 2 emptied the list — with the stronger must-NOT criterion standing in its place.
   - Proved the batch rewrite surgical: reverse-substituting `` `dpm_ `` → `` `mcp__plugin_dpm_dpm__ `` and diffing every body against released v0.7.0 gives **0 differing lines for all 22 skills and `shared/skill-conventions.md`, 6 for `spec`** (3 intentional line pairs).
   - Replaced an unreliable LLM-driven end-to-end check with a deterministic probe that reads the host's own `skill`, `tool` and `mcp` registries and executes a tool by its host-dispatched id.
   - Verified the whole batch in a fresh scratch project: 23 dpm skills, all `dpm-` prefixed, locations inside the package, MCP connected, 195/183 tools, **zero unresolved tool names**, zero legacy prefixes, zero `/dpm:` refs, 24 shared refs all absolute and existing, 23 descriptions naming the skill tool. Scratch deleted.
   - Reported honestly that the "gates" limb was never exercised, and let the user decide.
   - Deferred the peer's upstream narration changes with a stated reason (they would destroy the v0.7.0 oracle diff), and agreed with the peer that in this port those rules belong **inline** rather than in `shared/skill-conventions.md`, because the substituted conventions path is auto-rejected in non-interactive runs.

6. **All user messages:**
   - The compaction continuation instruction: *"Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with 'I'll continue' or similar. Pick up the last task as if the break never happened."*
   - The final instruction to produce this summary as text only, with no tool calls.

   *(The AskUserQuestion answer "Complete anyway, record the gap" arrived as a tool result. The two `claude-code-marketplace-0c` messages arrived as system-delivered cross-session messages, not user turns.)*

7. **Pending Tasks:**
   - **Resolve `ralph`** — this is the immediate blocker. `skills/ralph/SKILL.md` names `.claude/ralph-loop.local.md` five times and the stop-hook mechanism ~29 times. The hook was **never shipped even in released v0.7.0** (`hooks/` holds only `pre-commit`, and `ralph/SKILL.md` is the only file in the release naming `ralph-loop`), and v2 has no equivalent. This directly violates story 2's criterion 5 (`01M194VQ2QCJ4020HR42JXK3Y7`, `unit`, must_not): *"A skill body names a Claude Code mechanism — `mcp__plugin_`, `/dpm:`, `CLAUDE_PLUGIN_ROOT`, or `.claude/`."* Options drafted: (a) keep 23 registered, record criterion 5 unmet for ralph only (smallest amendment surface — recommended); (b) exclude ralph from registration via the profile seam (forces amending three criteria that say "twenty-three"); (c) rename the path cosmetically (defeats the criterion rather than satisfying it).
   - **Story 2 task 2** `01M194Y0SG5F09MSMQK4SM878N` — already satisfied by epic 01-02's `src/plugin/index.ts`; needs marking complete with the evidence.
   - **Story 2 task 3** `01M194Y2VA7BHW4QV9H1DT2094` — the scratch verification is **done** (results above); needs marking complete.
   - **Story 2 task 4** `01M194Y4AZD2TWHEPAT0VWDHY2` — write `tests/skill-port.test.js` for criterion 5 (criterion 6, no SQL, is already fully covered by `skills-corpus.test.js:69` with controls; criteria 1 and 3 are covered by `plugin-entry.test.js` lines 159, 222, 344).
   - Story 2's verification gate, refactoring pass, coverage, close, observation; then **stories 3, 4, 5**, the epic summary and retro.
   - **Deferred, explicitly not part of the port pass**: porting upstream dpm 0.7.2's narration changes (commits `0c1d501`, `265ec67`) — the plan-column narration sites in `do` (story-load bullet, Step 3), `ralph` (step 1b), `epics` (Step 3), plus the "Saying what a row holds" rule, to go **inline** rather than into `shared/skill-conventions.md`.
   - **Uncommitted working tree** (user commits, not me).

8. **Current Work:**

   Immediately before the summary request I had just sent the second `SendMessage` reply to `claude-code-marketplace-0c` (confirming opencode-dpm's database is intact, flagging the free-model bash incident as a possible shape for their rollback, and agreeing that the narration rules belong inline in this port).

   The batch pass is complete and the suite is **green at 1045 passing**. `grep -rn "/dpm:" skills shared` and `grep -rn "mcp__plugin_" skills shared` both return nothing. The only remaining Claude Code mechanism anywhere in `skills/` or `shared/` is:
   ```
   skills/ralph/SKILL.md:16:**`.claude/ralph-loop.local.md` is the plugin's file, not this one's.** It is how the stop hook knows
   skills/ralph/SKILL.md:158:`.claude/ralph-loop.local.md` changes every iteration and belongs in no commit.
   skills/ralph/SKILL.md:248:`.claude/ralph-loop.local.md`**. That is what the hook itself does on a matched promise and on
   skills/ralph/SKILL.md:290:1. **If `.claude/ralph-loop.local.md` already exists**, read it, report its iteration and the opening
   skills/ralph/SKILL.md:313:`.claude/ralph-loop.local.md`, and a `session` row carrying the run. The row is what survives the
   ```
   I was about to put the ralph decision to Chris via `AskUserQuestion`, having just established via the scratch probe that everything else in the batch verifies clean.

9. **Optional Next Step:**

   Put the `ralph` question to Chris via `AskUserQuestion`, then mark story 2's tasks 2 and 3 complete with their evidence and write `tests/skill-port.test.js` for criterion 5.

   This follows directly from the recorded plan, quoted verbatim from the epic section written in story 1 task 3: *"`ralph` names `.claude/ralph-loop.local.md` five times. That is a **Claude Code stop hook** — a file the harness reads to decide whether to re-enter the loop — and v2 has no equivalent mechanism. It is not a rename and it is not a path substitution; it is a missing capability, and the batch pass should meet it as a known decision rather than as a surprise. Leave those five references alone in story 2 and resolve them where the epic decides what `ralph` does under v2."*

   And from story 2's own criterion `01M194VQ2QCJ4020HR42JXK3Y7`: *"A skill body names a Claude Code mechanism — `mcp__plugin_`, `/dpm:`, `CLAUDE_PLUGIN_ROOT`, or `.claude/`."* (polarity `must_not`, approach `unit`) — which `ralph` is the only violator of, and which cannot be verified either way without the decision.
</summary>
