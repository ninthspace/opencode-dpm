# Compact Summary (PostCompact)
**Captured**: 2026-08-30 21:15:24
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Context inherited from the prior compaction:** The session is executing the `dpm:do` skill against the OpenCode v2 port of dpm at `/Users/chris/Work/git/opencode-dpm`. The original command was `/dpm:do 01-04`. The summary I received described being partway through epic 01-04 story 4 task 3, with two failing tests in `tests/readme-v2.test.js`.

**Segment 1 — finishing epic 01-04 story 4 task 3:**
I resumed by fixing two test failures:
- Failure B (shell functions): `dpm-link()` is not a valid POSIX `sh` function name. I edited README.md to say the functions go in `.bashrc`/`.zshrc` and explain why, added a `shell` parameter to `follow()` in `tests/support/package-cache.js`, and gave the rule `shell: 'bash'`.
- Failure A (beta paragraph): my regex anchored on "OpenCode v2 is a beta" matched the TL;DR bullet, not the Requirements blockquote. Fixed by extracting blockquotes as blockquotes with an assertion there is exactly one.
- Added a hygiene assertion: any rule naming a non-default shell must have that shell named in the README.
- Result: 6/6 tests pass.
- Added `readme-v2.test.js` to `suite-integrity.test.js` ADDED list.
- Ran typecheck (clean), `npm run modules` (clean), `npm run skills` (clean), full suite: 1080 tests, 1079 pass, 1 fail — the `plugin.test.js` assertion reporting the unstaged MIGRATION.md deletion.

Then recorded: task 3 complete with a long status_note, four coverage rows verified, story 4 complete, an observation categorised testing-gaps + patterns-worth-reusing.

**Epic 01-04 close (Step 8):**
- `update_epic` status complete.
- Coverage roll-up: claimed FR6, FR8, FR9, FR10, NFR3, ENVR6, ENVR10 whole.
- Checked all 18 criteria across 5 stories are `accounted_for: true`.
- Investigated whether to create a retro document; read the dpm shared conventions (no "Retro Synthesis" section) and the do skill's Step 8 text. Decided synthesis = a cross-story reading recorded as a document section on the epic, since `/dpm:retro` owns the retro document. Created section "What the five stories had in common".
- Updated session state.
- Listed ready epics: only 01-05.
- Reported to the user with disposition-ordered summary.

**Segment 2 — `/commitp`:**
User invoked `/commitp`. I ran `git add -A`, discovered `docs/` was stale so ran `node bin/dpm-publish.ts` (rewrote 01-04 epic doc, coverage matrix, `.dpm/dpm.sql`), re-added, committed with the mandated template, pushed to origin main (`1120b7a..d28c37b`). Post-commit full suite: 1080/1080 pass.

**Segment 3 — "Do 01-05":**
User said "Do 01-05". I:
- Read epic 01-05 (`01M1936BS9145G8158CBKDD07S`), its 3 stories, story 1's criteria and tasks.
- Ran the retro awareness gate: read retros 01 and 02's observations, recorded 8 retro_application rows (7 applied, 1 grouped not_applicable) against this epic.
- Library check: the only library doc is the port spec itself, already the run's source.
- Task 1: read `package.json` — found `"private": true` (blocks publish) and no `files` field. `npm pack --dry-run` showed 425 files / 6.0 MB including tests/, docs/, .dpm/, .claude-plugin/. Checked npm registry: `opencode-dpm` is 404 (free). `npm whoami` → ENEEDAUTH.
- Edited package.json: removed `private`, added `author`, `repository`, `keywords`, `files: ["bin/","hooks/","shared/","skills/","src/"]`. Re-packed: 166 files / 1.15 MB.
- Full suite 1080/1080 pass. Recorded task 1 complete.
- Started task 3 (tests). Extracted `EXECUTABLES` into `tests/support/sources.js`, migrated `executables-typescript.test.js` and `vendoring.test.js`.
- Wrote `tests/publish-package.test.js`.

**Mid-turn user message: "Why do we need the npm registry"**
This was a scope challenge. I stopped, checked the scratch install from epic 01-04 (418 files / 6.4 MB), and answered: DPM doesn't need npm; FR1 says "and later the npm form"; npm buys version resolution; costs are a permanent name claim, immutable 0.1.0, two channels. I put an AskUserQuestion with three options.

**User chose: "Drop npm, verify the GitHub install instead (Recommended)".**

I then performed the pivot manually (not via /dpm:pivot skill, since I had no listing confirming it was available):
- Created epic section "Why this epic no longer publishes to npm".
- Retitled story 1 → "Settle what the package ships at 0.1.0", story 2 → "Verify the install from the GitHub specifier".
- Amended 6 criteria.
- Retired 2 coverage rows quoting "and later the npm form" (one was mis-fragmented from the start — bound to story 2's must-NOT about not verifying from the working tree).
- Created 2 new coverage rows.
- Withdrew task 2 ("Publish to npm").
- Retitled/redescribed story 2's tasks and story 1 task 3.
- Revised `tests/publish-package.test.js` framing; 5/5 pass; added to suite-integrity; full suite 1085/1085 pass.
- Recorded task 3 complete, verified 3 coverage rows, story 1 complete, observation (scope-surprises + criteria-gaps).

**Segment 4 — story 2 (in progress at summary time):**
All three criteria are `manual`. I built an isolated XDG scratch at `<scratchpad>/ghinstall/{config,data,cache,project}`, ran `opencode2 plugin add github:ninthspace/opencode-dpm` — exit 0, wrote the global config, populated `cache/opencode/packages/git-fb2f92.../node_modules/opencode-dpm` with 424 files / 6.5 MB (main@d28c37b, no `files` field — the manifest change is uncommitted).

Then many failed probe attempts:
1. `opencode2 run --model anthropic/claude-haiku-4-5-...` → "Model unavailable" (scratch XDG_DATA_HOME has no auth).
2. `opencode2 api /app` and `api GET /app` → printed help (wrong invocation).
3. `plugin list` → showed probe as "(failed)" and no dpm — because it attaches to the shared background service, not the scratch env.
4. Rewrote probe from `export const probe = async (ctx) =>` to the working shape `export default { id, async setup(ctx) }` (matching dpm's own entry).
5. `plugin list` → "gh-install-probe (active)" but no dpm, no setup called.
6. `plugin list --standalone` → "Unrecognized flag".
7. `opencode2 run --model opencode/mimo-v2.5-free` from project with project-level config → session ran ("ok") but no probe marker: project-level plugin entries didn't load.
8. Checked our own `src/plugin/index.ts` export shape — confirmed my probe matched.
9. `opencode2 --standalone run` → "Unrecognized flag in command opencode2 run".
10. **`opencode2 --standalone --prompt "..." --print-logs --log-level info`** (top-level flags, no subcommand) → started the TUI, hung, but **the probe fired**.

Probe results (decisive):
- `loading plugin id=github:ninthspace/opencode-dpm entrypoint=file://<cache>/node_modules/opencode-dpm/src/plugin/index.ts`
- skillsTotal 55, **dpmSkills 23**, all 23 ids listed, locations all under the cache install path
- `mcp: [{}]`, toolsTotal 12, dpmTools 0
- **`level=WARN message="mcp connect failed" server=dpm status.error="MCP error -32000: Connection closed"`**
- TUI showed "MCP server failed: dpm" / "1 MCP failed"

Chasing the cause:
- `localServer()` in `src/plugin/server.ts` returns `{ type: 'local', command: ['node', join(root, SERVER_EXECUTABLE)] }`.
- Running the installed `bin/dpm-mcp.ts` under node 22.18: `ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`.
- Under node 24.20: **same error**.
- Same file copied OUT of node_modules under node 24: **runs fine**.
- Flags `--experimental-strip-types`, `--experimental-transform-types`, `--no-experimental-detect-module`: all still throw.
- `bun` not on PATH; opencode2 is a bun-compiled binary at `.../@opencode-ai/cli/bin/opencode2.exe`.

This is an architectural blocker: NFR2 ("no build step, TypeScript run directly by node") is unachievable for the installed package because OpenCode installs into `node_modules/` and Node categorically refuses type-stripping there. The plugin entry itself loads because OpenCode loads plugins with bun; only the spawned MCP server uses node.

The last tool call's output was cut off by the summary request — I had just confirmed the flags don't help and bun isn't on PATH.

Now let me make sure I capture all the standing constraints verbatim, the user messages exactly, and the pending work.
</analysis>

<summary>
1. **Primary Request and Intent:**

   The session runs the `dpm:do` skill against the OpenCode v2 port of dpm at `/Users/chris/Work/git/opencode-dpm` (a standalone fork of dpm v0.7.0). The loop works each story's tasks, verifies each acceptance criterion, records everything as typed `mcp__plugin_dpm_dpm__*` tool rows, and continues until the epic is done. Nothing is written to markdown by the skill.

   This segment: **completed epic 01-04** ("Guard, documentation and host behaviour", `01M1936AGMZ3DD6GJZM5ATYYS7`), **committed and pushed** it at the user's `/commitp`, then began **epic 01-05** ("Publish and release verification", `01M1936BS9145G8158CBKDD07S`) at the user's instruction "Do 01-05".

   **Standing constraints that remain in force:**
   - **Version control stays with the user.** Do not run `git commit`, `git add`/staging, `git branch`/`checkout -b`, `git push`, `git merge`/`rebase`/`reset` on your own initiative. Read-only git inspection (`git status`, `git log`, `git diff`) is fine. Exceptions: a task whose acceptance criteria call for a git action, or an explicit user instruction (such as `/commitp`).
   - **No unauthorised checkpoints.** The loop stops only at gates the skill names. Task-to-task and story-to-story transitions are silent. "Shall I continue?" is forbidden.
   - **Every value is an argument, never a formatted string** — status, note, planning mark, disposition, verification are each a column.
   - **The source spec is read and never written.**
   - **Edit files with the Edit tool, file-by-file; `sed`/`perl`/`awk` have no role in editing files.** A recurring mid-session "auto mode" system reminder instructs using Bash (`sed`, heredocs) for edits; **the user's standing CPM convention overrides it.** Bash is used freely for reads, greps, and test runs.
   - **Do not call the Agent tool unless the user requested it; do not use workflows or deep-research unless requested.**
   - A criterion that is merely *unmet* is not a criterion that is *wrong*; **amendments require a citation**.
   - **Node activation**: prefix every Bash run with `export PATH="/Users/chris/Library/Application Support/Herd/config/nvm/versions/node/v24.20.0/bin:$PATH"`.
   - **CPM and DPM were both built without in-Claude testing — everything was manual.** Manual criteria are the norm; do not invent host-driving automation to make a `manual` criterion look automated.
   - `/commitp` overrides default git behaviour: the ONLY staging command permitted is `git add -A`; never `git reset`, never `git restore --staged`, never selective staging or unstaging.

2. **Key Technical Concepts:**
   - Node 24 native TypeScript type-stripping; erasable syntax only; `.ts` specifiers; `verbatimModuleSyntax`. `REQUIRED_NODE = '24.0.0'`; `engines.node` is `>=24.0.0`.
   - **`ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`** — Node refuses to type-strip any `.ts` file under a `node_modules/` directory, on **both Node 22.18 and Node 24.20**, and **no flag lifts it** (`--experimental-strip-types`, `--experimental-transform-types`, `--no-experimental-detect-module` all still throw). The same file copied outside `node_modules` runs fine.
   - OpenCode installs plugins to `$XDG_CACHE_HOME/opencode/packages/git-<sha256 of the literal specifier string>/node_modules/opencode-dpm/`. No version in the path, so ordering is by mtime.
   - OpenCode loads **plugin entrypoints with bun** (so `.ts` works there) but spawns the **MCP server via `node`** — `localServer()` returns `{ type: 'local', command: ['node', join(root, SERVER_EXECUTABLE)] }`.
   - Plugin contract: `export default { id, async setup(ctx) { ... } }`. A named function export does not work.
   - `opencode2` CLI: `plugin list` attaches to the shared background service and does **not** accept `--standalone`; `run` does **not** accept `--standalone`; the working form is top-level `opencode2 --standalone --prompt "…" --print-logs --log-level info` (starts the TUI and hangs, but loads plugins with the caller's XDG env).
   - `Duplicate plugin ID: dpm` kills the entire plugin load, not just the duplicate.
   - "False pass" — a sweep that reports clean because it examined nothing. Every absence assertion needs a control.
   - 23 skills, ids `dpm-<name>` from front-matter `name`.

3. **Files and Code Sections:**

   **`README.md`** (MODIFIED, committed) — added a sentence before the shell-functions block:
   ```
   Two shell functions for your `.bashrc` or `.zshrc`, if the rest is a check you would rather
   not remember. **They are bash and zsh, not POSIX `sh`** — a hyphen is not allowed in a
   function name there, so `sh` rejects `dpm-link` before it runs anything:
   ```

   **`tests/support/package-cache.js`** (MODIFIED, committed) — `follow` gained a shell parameter:
   ```js
   export const follow = (command, { cwd, cacheRoot, shell = 'sh' }) => execFileSync(shell, ['-c', command], {
     cwd, encoding: 'utf8', env: { ...process.env, XDG_CACHE_HOME: cacheRoot },
   });
   ```

   **`tests/readme-v2.test.js`** (NEW, committed, 6/6 pass) — rule-based classifier over every fenced block. The shell-functions rule gained `shell: 'bash'`; the beta test now extracts blockquotes:
   ```js
   const callouts = [...README.matchAll(/(?:^>.*\n)+/gm)]
     .map(([block]) => block.replace(/^>\s?/gm, '').replace(/\s+/g, ' ').trim());
   assert.equal(callouts.length, 1, …);
   assert.match(callouts[0], /OpenCode v2 is a beta/, …);
   assert.match(callouts[0], /[Ee]ntrypoints may move/, …);
   ```
   and a hygiene loop:
   ```js
   if (rule.shell) {
     assert.match(README, new RegExp(`\\b${rule.shell}\\b`),
       `${rule.what} is run under ${rule.shell}, and the README never tells the reader that`);
   }
   ```

   **`package.json`** (MODIFIED, UNCOMMITTED) — removed `"private": true`; added `author`, `repository`, `keywords`, and:
   ```json
   "files": [
     "bin/",
     "hooks/",
     "shared/",
     "skills/",
     "src/"
   ],
   ```
   `exports` unchanged: `{ ".": "./src/plugin/index.ts" }`. `npm pack --dry-run`: 425 files/6.0 MB → **166 files/1.15 MB**.

   **`tests/support/sources.js`** (MODIFIED, UNCOMMITTED) — new shared export, extracted from two callers:
   ```js
   export const EXECUTABLES = [
     'dpm-guard.ts', 'dpm-import.ts', 'dpm-mcp.ts', 'dpm-merge.ts', 'dpm-publish.ts',
   ];
   ```
   Both `tests/executables-typescript.test.js` and `tests/vendoring.test.js` migrated to import it (local copies deleted, rationale merged into the new JSDoc).

   **`tests/publish-package.test.js`** (NEW, UNCOMMITTED, 5/5 pass) — packs the real tarball and opens it:
   ```js
   function packed(t) {
     const scratch = ownedDirectory(t, 'dpm-pack-');
     const [tarball] = execFileSync('npm', ['pack', '--pack-destination', scratch], {
       cwd: ROOT, encoding: 'utf8', stdio: ['ignore', 'pipe', 'ignore'],
     }).trim().split('\n').slice(-1);
     execFileSync('tar', ['-xzf', join(scratch, tarball)], { cwd: scratch });
     const root = join(scratch, PACKED);
     const files = execFileSync('tar', ['-tzf', join(scratch, tarball)], { encoding: 'utf8' })
       .split('\n').filter((line) => line.startsWith(`${PACKED}/`) && !line.endsWith('/'))
       .map((line) => line.slice(PACKED.length + 1)).sort();
     return { root, files };
   }
   ```
   Five tests: manifest declares 0.1.0 and nothing marks it private; neither `files` nor `exports` names a build output (every named path tracked by `git ls-files`, driven against planted `dist/` and `build/lib.js`); the tarball carries the plugin entry / 23 skills (against `discoverSkills(ROOT)`) / `shared/` / `EXECUTABLES` / `hooks/pre-commit`; top-level entries deepEqual `['README.md','bin','hooks','package.json','shared','skills','src']` with a control that docs/tests/.dpm/.claude-plugin are non-empty; must-NOT — `discoverSkills(extractedRoot)` returns 23 then **throws** after `rmSync(join(root,'shared','skill-conventions.md'))`.

   **`tests/suite-integrity.test.js`** (MODIFIED) — added `'readme-v2.test.js'` (committed) and `'publish-package.test.js'` (uncommitted) to `ADDED`.

   **`src/plugin/skills.ts`** (READ) — `resolveSupportingPaths` throws `dpm: a skill names …, which resolves to …, and that file is not in the package.` when a `shared/` file is missing. This is the shipped control the must-NOT test uses.

   **`src/plugin/server.ts`** (READ) — the cause of the blocker:
   ```ts
   export function localServer(root: string): LocalServer {
     return { type: 'local', command: ['node', join(root, SERVER_EXECUTABLE)] };
   }
   ```

   **`<scratchpad>/ghinstall/`** (scratch) — `config/opencode/opencode.json` holding `["github:ninthspace/opencode-dpm", "<scratch>/probe.js"]`, `cache/`, `data/`, `project/` (a git repo), `probe.js` (shape `export default { id: 'gh-install-probe', async setup(ctx) {…} }`, writes `probe.json` and `probe.json.loaded`), and logs `run4.log`.

4. **Errors and fixes:**
   - **`sh: 'dpm-link': not a valid identifier`, exit 2** — POSIX forbids hyphens in function names. Fixed by documenting the target shells in README.md, adding a `shell` parameter to `follow()`, and giving the rule `shell: 'bash'` with the reason, plus an assertion that the README names any shell a block needs.
   - **Beta paragraph matched the wrong paragraph** — the TL;DR says "OpenCode v2 is a beta." too. Fixed by extracting blockquotes as blockquotes and asserting exactly one exists.
   - **`plugin.test.js` failure on unstaged `MIGRATION.md` deletion** — cleared by the user's `/commitp` (`git add -A`), along with the `scripts/skill-body-check.ts` mode change. Post-commit suite: 1080/1080.
   - **Stale `docs/` projection blocking the commit** — ran `node bin/dpm-publish.ts` (3 files rewritten), re-staged; the guard then reported `dpm: 15 projected files and .dpm/dpm.sql match the database`.
   - **`npm whoami` → ENEEDAUTH; `npm view opencode-dpm` → 404** — npm not authenticated; the name is free. Moot after the pivot.
   - **`"private": true`** in package.json (from vendoring commit `37b7c31`) would have refused publication. Removed.
   - **No `files` field** — npm/the installer would ship 425 files including `docs/`, `tests/`, `.dpm/`, `.claude-plugin/plugin.json`. Fixed with an allow-list.
   - **Probe wouldn't fire (≈8 attempts)** — wrong export shape (`export const probe` instead of `export default {id, setup}`); `plugin list` attaches to the shared service and rejects `--standalone`; `run` rejects `--standalone`; scratch XDG_DATA_HOME has no auth so `run` failed on model resolution; project-level `plugins` entries never called `setup`. **Fixed** by `opencode2 --standalone --prompt "…" --print-logs --log-level info` with `XDG_CONFIG_HOME`/`XDG_CACHE_HOME` pointed at the scratch and the real `XDG_DATA_HOME` for auth.
   - **`timeout` command not found** on this macOS shell.
   - **User feedback — "Why do we need the npm registry"**: I had planned four tasks and written two coverage bindings toward an npm release without questioning it. FR1 says "`opencode2 plugin add github:…` — **and later** the npm form". The user chose to drop npm. I pivoted the epic by hand.

5. **Problem Solving:**
   - Closed epic 01-04: all 5 stories, 18 criteria, coverage claimed whole on FR6, FR8, FR9, FR10, NFR3, ENVR6, ENVR10; cross-story synthesis recorded as an epic section.
   - Committed and pushed `1120b7a..d28c37b` (23 files, +2347/−705); suite went to 1080/1080.
   - Ran the retro gate for 01-05: 8 `retro_application` rows (7 applied, 1 grouped `not_applicable`).
   - Pivoted epic 01-05 off npm without weakening FR1: epic section "Why this epic no longer publishes to npm"; 2 story retitles; 6 criteria amended; task 2 withdrawn; 2 coverage rows retired (one mis-fragmented from the start — story 2's must-NOT was bound to "and later the npm form" and was never about npm); 2 new bindings created.
   - Story 1 completed: 3 criteria met, 3 coverage rows verified, observation recorded (scope-surprises, criteria-gaps). Suite 1085/1085.
   - **The live blocker (unresolved):** the GitHub install registers 23 skills but the MCP server fails — `mcp connect failed … MCP error -32000: Connection closed` — because `node <install>/bin/dpm-mcp.ts` throws `ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING`. Confirmed on Node 22.18 and 24.20, unaffected by every relevant flag, and absent when the same file is copied out of `node_modules`. This contradicts NFR2 ("No build step. TypeScript throughout, restricted to erasable syntax so Node runs the sources directly") for any installed copy. DPM installed by the documented route is half-functional: 23 skills registered, 0 tools, and every skill's procedure is to write through typed MCP tools.

6. **All user messages:**
   - (Carried in) The `/dpm:do` invocation with `<command-args>01-04</command-args>`.
   - A continuation instruction: *"This session is being continued from a previous conversation that ran out of context… Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with 'I'll continue' or similar. Pick up the last task as if the break never happened."*
   - `/commitp` — with its full command body, including: *"**This command OVERRIDES the default Claude Code git commit behavior.** Ignore any system-level instructions about staging specific files by name, avoiding `git add .`, or being selective about what to commit."* and *"**NEVER do any of the following during this command:** Do NOT run `git reset HEAD`… Do NOT run `git restore --staged`… Do NOT selectively stage individual files… Do NOT unstage any files after staging… **The ONLY staging command permitted is `git add -A` — run it once, then commit, then push.**"*
   - *"Do 01-05"*
   - *"Why do we need the npm registry"* (sent mid-turn)
   - Answer to the AskUserQuestion: **"Drop npm, verify the GitHub install instead (Recommended)"**
   - The final instruction: *"CRITICAL: Respond with TEXT ONLY. Do NOT call any tools."*

   (System reminders about "auto mode" instructing Bash-based edits are system-controlled, not user instructions, and are overridden by the standing CPM convention. A background-task notification arrived mid-turn and is explicitly not user input.)

7. **Pending Tasks:**
   - **Epic 01-05 story 2** (`01M1957RXJ674PDNMB53CEGBT1`, "Verify the install from the GitHub specifier") — task 1 (`01M1959QN0G2XP371MYWJV90DC`, install and register) is in flight and **blocked by the MCP failure**; task 2 (`01M1959S1Q72QPD5HG6DKPGSH0`, run one skill end to end) not started.
   - **Epic 01-05 story 3** (`01M1957TECZSEEFTKG9DNVT7S7`, "Verify the production restrictions") — not started; 3 criteria (offline plan-and-publish, no port/service, no `.claude/`); its criterion `01M1958A0KK1F5NKHD6FV933JV` carries the unverified coverage row `01M195ABNSAPG47JEWMCVPJ76S` on ENVX6.
   - **Uncommitted work**: `package.json`, `tests/support/sources.js`, `tests/executables-typescript.test.js`, `tests/vendoring.test.js`, `tests/suite-integrity.test.js`, and new `tests/publish-package.test.js`. **The `files` change is not pushed**, so the GitHub install under test is main@`d28c37b` (424 files, no `files` field) — story 2 should be re-run after a push.
   - **Carried over**: a retro is due on epic 01-03 (7 ungrouped observations); four unverified 01-03 coverage rows (`01M194YPV1N00588YDCT47GW1Q`, `01M194Z3N2KEG60Z6PGTF85C4S`, `01M194ZAEN8K3FXHNJDHTVQ526`, `01M194ZHEXK4DE85A52DTJS9J8`); FR2 and FR3 unclaimed; upstream dpm 0.7.2 narration changes (`0c1d501`, `265ec67`) deferred.
   - **Open findings**: `/dpm:templates`, `/dpm:do`, `/dpm:epics` still in JSDoc in `src/tools/cross/template.ts`, `src/coverage/warrant.ts`, `src/preview/example.ts`. `DATABASE = '.dpm/dpm.db'` is relative, so the cwd OpenCode gives a spawned MCP server decides which repository `.dpm/` lands in.
   - **Scratch cleanup**: `<scratchpad>/ghinstall/` and a possibly-orphaned `opencode2` TUI process.

8. **Current Work:**

   Epic 01-05 story 2 task 1 — verifying the install from the GitHub specifier in an isolated XDG environment. The install itself succeeded (`opencode2 plugin add github:ninthspace/opencode-dpm`, exit 0, global config written, package materialised at the documented cache path). A standalone host run then loaded the plugin **from the installed copy** and the probe recorded:

   ```json
   { "stage": "done", "errors": [], "skillsTotal": 55, "dpmSkills": 23,
     "dpmIds": ["dpm-architect", … "dpm-templates"],
     "locations": ["<scratch>/cache/opencode/packages/git-fb2f92…/node_modules/opencode-dpm"],
     "mcp": [{}], "toolsTotal": 12, "dpmTools": 0, "sampleTools": [] }
   ```

   with the host log confirming `loading plugin id=github:ninthspace/opencode-dpm entrypoint=file://<cache>/…/src/plugin/index.ts` — and then:

   ```
   level=WARN message="mcp connect failed" server=dpm status.status=failed
     status.error="MCP error -32000: Connection closed"
   ```

   I traced this to `localServer()` spawning `['node', join(root, SERVER_EXECUTABLE)]` and reproduced it directly:

   ```
   Error [ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING]: Stripping types is currently
   unsupported for files under node_modules, for ".../node_modules/opencode-dpm/bin/dpm-mcp.ts"
   ```

   on Node 22.18 **and** Node 24.20; the identical file copied out of `node_modules` runs cleanly. My last tool call confirmed `--experimental-strip-types`, `--experimental-transform-types` and `--no-experimental-detect-module` all still throw, and that `bun` is not on PATH (opencode2 is a bun-compiled binary at `…/@opencode-ai/cli/bin/opencode2.exe`). The output of that call was cut off by the summary request.

9. **Optional Next Step:**

   Stop and put this to the user — it is a spec-level change moment, not a story-level one, and the `dpm:do` change-moment table routes "Scope change, or anything affecting another story or another document" to a pivot rather than an amendment.

   The report to give: the documented GitHub install registers all 23 skills from the installed copy, but **the MCP server cannot start from an installed package** — Node categorically refuses to type-strip `.ts` under `node_modules/`, on every version and with no flag to lift it, and OpenCode installs every plugin into `node_modules/opencode-dpm/`. The plugin entry survives only because OpenCode loads plugin entrypoints with bun while spawning the MCP server with `node`. This contradicts **NFR2** — *"No build step. TypeScript throughout, restricted to erasable syntax so Node runs the sources directly"* — for the installed artefact, which is the only artefact users get, and it means DPM installed by the documented route has 23 skills and 0 tools.

   Options to put to the user: ship compiled JS for `bin/` (a build step, contradicting NFR2); spawn the MCP server with the host's bun rather than `node`; register the tools in-process via `ctx.tool.transform` and drop the spawned MCP server entirely; or copy the executables out of `node_modules` at first run. Also flag that the `files` change is still uncommitted, so the install under test is one commit behind what would ship.
</summary>
