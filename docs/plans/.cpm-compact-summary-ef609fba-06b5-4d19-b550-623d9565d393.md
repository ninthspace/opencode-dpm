# Compact Summary (PostCompact)
**Captured**: 2026-09-02 17:36:42
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Session origin**: This is a continuation of a compacted `/dpm:do` run in `/Users/chris/Work/git/opencode-dpm`. The first user message was the standard resume instruction. The prior summary established: epic 02-05 "The v1 walk" (`01M1C6E3XG8EVENPG8P74RHGGA`), story 2 complete, story 3 pending.

**Phase 1 — Story 3 setup**: I read story 3's criteria (four `manual` criteria), its row, its three tasks, found the session id (`ef609fba-06b5-4d19-b550-623d9565d393`), and updated the session phase/state with the full loop contract.

**Phase 2 — The walk itself**: I built a fresh throwaway project at `<scratchpad>/walk`, followed the README's install verbatim (two config keys, `ln -s` hook, `git config core.hooksPath`), started `opencode serve --port 4599`, confirmed `/mcp` = connected and 54 skills of which 23 `dpm-*`, 0 errors. Then ran a real `opencode run --auto` session invoking `dpm-quick`. It took ~20 minutes on the local MTPLX 27B model but completed the whole quick: session row `dpm:quick`/`complete`, quick document `01M1GKVHBGP8VQXZ8PWT8DS9CP` "Add --verbose flag to the deploy script", three criteria all met, an observation, `deploy.sh` written.

**Phase 3 — The commit half**: Attempted commit with nothing published → guard refused naming both artefacts and the absolute publish command (this was the control). Published → committed `7d2a28a`. Then hand-edited the generated doc → guard refused, edit left in place. Then republished to restore.

**Phase 4 — The deny rule, and the defect**: First attempt used the README's `permissions` array shape → **`Error: Configuration is invalid ... ↳ V2 permissions are not supported by OpenCode V1. Use V1 "permission" rules or run opencode2.`** The session would not start at all. This is the central finding of story 3.

I then read v1's real permission schema out of the strings dump at `<scratchpad>/v1strings.txt` lines 294495–294540, which gave the authoritative shape: `"permission": { "edit": "deny", "bash": {...} }`, actions allow/ask/deny, bare action is shorthand for `{"*": action}`, insertion order matters with LAST match winning, known keys list, and "Per-agent `permission:` overrides top-level `permission:`" (opposite of what the README claimed).

Probes run live:
- `"skill": {"dpm-*": "deny"}` → refusal quoting the ruleset. (First retry failed because the model called the skill tool with `id` instead of `name`; second retry with an explicit prompt worked.)
- Without the rule → skill body returned.
- `"dpm_read_shared_document": "deny"` → tool removed from the model's declared set entirely.
- `"dpm_*": "deny"` → "NONE" dpm tools.
- The corrected README block (`skill: {dpm-*: allow}`, `dpm_*: allow`, `dpm_publish: ask`) → skill loads, 184 dpm tools.

**Phase 5 — README rewrite**: Six Edits to README.md replacing the evaluation-model paragraph, the `external_directory` historic reference, the action/resource table, both JSON blocks, and adding a refusal-quote block.

**Phase 6 — Test repairs**: Two new RULES in `readme-v2.test.js` for the two new unlabelled fenced blocks; new subject in `readme-host.test.js`; then two failures:
1. The control test — my `carries: /"permission":\s*\{/` matched twice so stripping one left the other. Fixed by narrowing to `/"permission":\s*\{\s*"skill":\s*\{\s*"dpm-\*":\s*"allow"/`.
2. The `OTHER_HOST` must-NOT sweep — my prose named "OpenCode 2" and the verbatim error names `opencode2`. Fixed two ways: rewrote prose to "the host's next major version", and added a lookahead exemption `/^(?![^\n]*V2 permissions are not supported)[^\n]*\bopencode2\b/m` mirroring the existing `XDG_CACHE_HOME` exemption.

**Phase 7 — The bigger finding**: A full `npm test` revealed 4 failures in `tests/permission-entries.test.js` — which I had not known existed. Its `documented()` read `JSON.parse(block).permissions ?? []`. That `?? []` is why ten green tests never saw the wrong shape. I rewrote the file onto v1 vocabulary (`key`/`pattern`/`effect`), added `refusedBlocks()`, and drove both new checks red first (by temporarily breaking README and reverting).

Note: I mistakenly reported "84 failures" from one bad `npm test` run early on; a clean rerun showed 5 (4 mine + the known unstaged-deletion one). I did not dwell on it.

**Phase 8 — Refactoring pass (5b)**: Created `tests/support/readme.js` holding `README`, `configBlocks()`, `REFUSED_KEY`, `refusedBlocks()`, and pointed both `permission-entries.test.js` and `readme-v2.test.js` at it.

**Phase 9 — Recording**: three `update_task` completes, four `update_coverage` verifications, `update_story` complete, `create_observation` `01M1GPFKRN3C5P28R2PZB7G642` with three categories, `update_epic` complete, thirteen `update_requirement` coverage claims, `create_retro` 10 (`01M1GPS7MS4BPFANKN7X9NA36A`), three `update_observation` synthesis writes, `update_session`.

**Phase 10 — Publish**: `node ~/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.1/bin/dpm-publish.js` → 1 new, 3 rewritten.

**Phase 11 — IP question**: An unrelated user question. Answered: public `194.140.214.134`, local en0 `192.168.0.204`.

**Phase 12 — /commitp**: User invoked the slash command, which explicitly overrides my default "version control stays with the user" constraint. `git add -A`, verified 1179/1179 green, committed `03afa03`, pushed to `origin/main` (`b9b5ea5..03afa03`).

**Phase 13 — "What's next?"**: I listed all ten epics complete, recommended the projection "Blocked by" inversion bug first, then retro-10 lesson promotion, spec-02 revision, develop-DPM workflow regression, FR9/FR10.

**Phase 14 — "Yes"**: The user agreed to take the projection bug. I began reading the code:
- `src/projection/load.ts:112` — `storyDependencies: { table: 'dependency', parent: 'source_story_id', order: ['kind', 'id'] }`
- `src/projection/templates/epic.ts:92-94` — `const blockedBy = row.dependencies.filter(...).map((edge) => edgeTarget(db, edge, identifiers, epicId))`
- `edgeTarget` at lines 49-64, which resolves `edge.target_story_id` / `edge.target_document_id`

That's where I am. I have NOT yet made any edit for this bug.

Important nuance I noticed while reading and must record: `epic.ts:92` reads `row.dependencies`, not `row.storyDependencies`. That needs verifying — the collection key used by the story renderer may differ from what I assumed. Both `dependencies` (parent `source_document_id`) and `storyDependencies` (parent `source_story_id`) exist in COLLECTIONS. I need to check how the story row's collections are attached before editing.

Security/process constraints to preserve: the CPM Implementation Guidelines (Edit tool not sed/perl/awk for repo edits; version control stays with the user EXCEPT where explicitly directed — /commitp is such a direction); the source spec is read never written; don't call AgentTool unless requested; no workflows/deep-research unless requested; user email used only for identification; the standing auto-mode injected instruction to use Bash+sed for edits which I resolve in favour of Edit/Write for repo files; credential redaction when printing config; address the user as Chris.

Let me make sure I capture the exact code snippets I have verbatim, and the exact new test code I wrote, since those are needed to continue.
</analysis>

<summary>
1. **Primary Request and Intent:**

   This session began as a continuation of one long `/dpm:do` run in `/Users/chris/Work/git/opencode-dpm`, working epic **02-05 "The v1 walk"** (`01M1C6E3XG8EVENPG8P74RHGGA`) to completion. The `dpm:do` contract holds that the run executes every story in **one continuous loop**, stopping only at named gates (unmet criteria, unroutable approach tag, external blocker, ambiguous criterion, change moment, epic-end retired-story question) — "**Any prompt asking whether to carry on is an unauthorised checkpoint.**"

   Work completed in sequence:
   - **Story 3 "The end-to-end walk"** (`01M1CB2AHDAQ1JRMMJYB23QH4P`) — driven as one continuous trip per a retro disposition, against a real OpenCode 1.18.25 host.
   - **Epic close** — status, coverage roll-up over spec 02, retro 10.
   - **Publish**, then `/commitp` (commit + push).
   - An unrelated one-off: the user asked for their IP address.
   - **Current task**: the user asked "What's next?", I recommended fixing the projection's "Blocked by" inversion bug, and the user said **"Yes"**.

2. **Key Technical Concepts:**

   - **dpm** — SQLite-backed planning system exposed as MCP tools (`mcp__plugin_dpm_dpm__*`). Specs → requirements → epics → stories → tasks → criteria → coverage rows binding criteria to verbatim spec fragments. `docs/` is a one-way generated projection; `.dpm/dpm.sql` is the committed text form of the database.
   - **opencode-dpm** — dpm ported from Claude Code to OpenCode v1. Plain Node ESM TypeScript run natively by Node ≥24 type-stripping. No build step, no loader, zero runtime dependencies.
   - **The v1 install surface (settled earlier this run by Chris at a change moment)**: `plugin: [<clone>/src/plugin/index.ts]` + `skills: [<clone>/skills]`. The `plugins` config key is stripped by v1's config layer, so the object plugin route is unreachable.
   - **v1's permission model — the central discovery of this session.** The key is `permission` (singular, an object). Value per key is either a bare action string (shorthand for `{"*": action}`) or `{pattern: action}`. Within an object **insertion order matters and the LAST matching pattern wins**. A per-agent `permission` **overrides** the top-level one. Known keys: `read, edit, glob, grep, list, bash, task, external_directory, todowrite, question, webfetch, websearch, lsp, doom_loop, skill`; `todowrite, question, webfetch, websearch, doom_loop` take only a flat action. **MCP tool names are also accepted as keys** even though they are not in that list.
   - **`permissions` (plural array of `{action, resource, effect}`) is OpenCode 2's shape and v1 refuses the entire configuration over it** — the session does not start.
   - **A denied `skill` refuses visibly, quoting the ruleset. A denied tool is removed from the model's tool set entirely, with nothing said.**
   - **`opencode serve` is the instrument** — stays up and answers `/config`, `/mcp`, `/skill`, `/doc` directly. `opencode run --auto` auto-approves permissions that are not explicitly denied, which makes it the right driver for the deny probes.
   - **The skill tool's argument is `name`, not `id`** — every dpm SKILL.md description says *"Invoke with the skill tool, id ..."*, which is slightly off for v1.
   - **`RUNTIME` is the bare string `node`** resolved off the **host's** PATH. Node 24 must be first: `export PATH="/Users/chris/Library/Application Support/Herd/config/nvm/versions/node/v24.20.0/bin:/Users/chris/.opencode/bin:$PATH"`.
   - **MTPLX** — Chris's local model daemon on 127.0.0.1:8000, model `mtplx/mtplx-qwen38-27b-optimized-speed`. Slow (~2 min per step).
   - **The `?? []` reading failure** — the transferable lesson: a reader with a default cannot report failing to find its subject. Floor assertions placed on the *result* of a reading rather than on the reading itself do not help.
   - **INHERITED/ADDED/REMOVED idiom** in `tests/suite-integrity.test.js`; the **planted-control** idiom (drive a check red before trusting it green); library lessons 02–05 ("An absence is only an observation when something was watching"; "Find the oracle before writing the expectation"; "Never match on a string another string can contain"; "Ask the host, not a test's idea of the host").

3. **Files and Code Sections:**

   **`README.md` — MODIFIED (Permissions section rewritten)**

   The section had shipped OpenCode 2's array shape through four stories of a v1-only README. Six edits:

   Replaced the evaluation-model paragraph with:
   ```
   **The key is `permission`, singular, and it holds an object.** Each entry is keyed by what
   is being done — `skill`, `bash`, `edit`, or a tool's own name — and its value is either a
   bare action (`"deny"`, which is shorthand for `{ "*": "deny" }`) or an object of
   `pattern: action`. Within one of those objects **insertion order matters**: OpenCode
   evaluates the **last** matching pattern, so broad rules go first and narrow ones last. A
   per-agent `permission` overrides the top-level one rather than being appended to it.

   **`permissions` — plural, an array of `{ action, resource, effect }` — is the shape the
   host's next major version takes, and v1 does not merely ignore it.** It refuses the whole
   configuration, and the session does not start:

   ```
   Error: Configuration is invalid at /path/to/opencode.json
   ↳ V2 permissions are not supported by OpenCode V1. Use V1 "permission" rules or run opencode2.
   ```

   That is the one failure in this file that is loud, and it is worth knowing which shape you
   are looking at before you copy anything: the two are close enough to read as variants of
   each other and only one of them starts.
   ```
   (Note: the prose deliberately says "the host's next major version" rather than "OpenCode 2", because `readme-host.test.js`'s `OTHER_HOST` must-NOT forbids naming the second host.)

   `{ "action": "external_directory", "resource": "*", "effect": "ask" }` → `"external_directory": { "*": "ask" }`.

   The table heading changed from `| What happens | Action | Resource |` to `| What happens | Key | Pattern |`, and gained:
   ```
   **The two fail differently, and the tool half fails quietly.** A denied `skill` refuses at
   the point of use and says so, quoting the rule that stopped it. A denied tool is **removed
   from the model's tool list before the session starts** — there is no refusal to read,
   because there is nothing left to call. `"dpm_*": "deny"` produces a session that reports
   having no DPM tools at all rather than one that is told it may not use them, which is why a
   tool rule is worth getting right the first time rather than debugging from the inside.
   ```

   Both JSON blocks rewritten:
   ```json
   {
     "permission": {
       "skill": { "dpm-*": "allow" },
       "dpm_*": "allow"
     }
   }
   ```
   ```json
   {
     "permission": {
       "dpm_publish": "ask"
     }
   }
   ```
   And the inline reference `{ "action": "skill", "resource": "dpm-publish" }` → `"skill": { "dpm-publish": "ask" }`.

   A new closing block:
   ```
   The refusal quotes the ruleset that produced it, which is what tells you a `dpm-*` pattern
   matched rather than something broader:

   ```
   The user has specified a rule which prevents you from using this specific tool call.
   Here are some of the relevant rules [{"permission":"skill","pattern":"dpm-*","action":"deny"}]
   ```
   ```

   **`tests/support/readme.js` — NEW** (the refactoring pass's consolidation)
   ```js
   import { readFileSync } from 'node:fs';
   import { join } from 'node:path';

   /** The document under test, read once. */
   export const README = readFileSync(join(import.meta.dirname, '..', '..', 'README.md'), 'utf8');

   export const configBlocks = (source = README) => [...source.matchAll(/```json\n([\s\S]*?)```/g)]
     .map(([, block]) => JSON.parse(block));

   export const REFUSED_KEY = 'permissions';

   export const refusedBlocks = (source = README) => configBlocks(source)
     .filter((parsed) => REFUSED_KEY in parsed);
   ```
   Its docblock records why two files check the same blocks at different scopes (whole README vs the Permissions section) and cross-references `tests/support/dpm-clone.js` for the same argument about `CLONE_PLACEHOLDER`.

   **`tests/permission-entries.test.js` — HEAVILY MODIFIED** (this is the file that should have caught the defect and did not)

   Its `documented()` was `JSON.parse(block).permissions ?? []`. Now:
   ```js
   function documented(source = permissions()) {
     return configBlocks(source)
       .flatMap((parsed) => Object.entries(parsed.permission ?? {}))
       .flatMap(([key, value]) => (typeof value === 'string'
         ? [{ key, pattern: '*', effect: value }]
         : Object.entries(value).map(([pattern, effect]) => ({ key, pattern, effect }))));
   }
   ```
   Vocabulary renamed throughout from v2's `action`/`resource`/`effect` to v1's `key`/`pattern`/`effect`. Two tests renamed: `'every skill pattern the README names resolves to a registered skill [unit]'` and `'every tool key the README names resolves to a registered tool [unit]'`. The well-formedness test gained:
   ```js
   assert.deepEqual(refusedBlocks(permissions()), [],
     'the section recommends a `permissions` array, which is the other host\'s shape — v1 refuses '
     + 'the whole configuration on it and the session does not start');

   assert.deepEqual(documented('```json\n{"permission":{"a":{"b":"maybe"},"c":"deny"}}\n```'),
     [{ key: 'a', pattern: 'b', effect: 'maybe' }, { key: 'c', pattern: '*', effect: 'deny' }]);

   const wrong = '```json\n{"permissions":[{"action":"skill","resource":"dpm-*","effect":"allow"}]}\n```';

   assert.equal(refusedBlocks(wrong).length, 1, 'the reading does not see the plural key in a block that uses it');
   assert.deepEqual(documented(wrong), [], 'a block in the refused shape yields rules, so this is not the trap it was');
   ```
   Imports now include `import { README, configBlocks, refusedBlocks } from './support/readme.js';` and the local `readFileSync` README const was removed. The `HOST_ACTIONS` planted control became `'```json\n{"permission":{"external_directory":{"*":"allow"}}}\n```'`. A long new docblock section headed `## Epic 02-05 story 3 — this file read the wrong shape, and so did the README` records the `?? []` failure.

   **`tests/readme-v2.test.js` — MODIFIED**

   Two new RULES inserted before `'the pre-commit framework entry'`:
   ```js
   {
     what: 'the configuration error a v2 permissions array produces',
     matches: ({ body }) => body.includes('V2 permissions are not supported'),
     run: false,
     why: 'it is output to recognise rather than a command to run, and producing it needs a host',
     check: (body) => {
       assert.match(body, /Configuration is invalid/, ...);
       assert.deepEqual(refusedBlocks(), [], ...);
       assert.ok(configBlocks().some((parsed) => 'permission' in parsed), ...);
     },
   },
   {
     what: 'the refusal a denied skill produces',
     matches: ({ body }) => body.includes('prevents you from using this specific tool call'),
     run: false,
     why: 'it is output to recognise rather than a command to run, and producing it needs a host',
     check: (body) => {
       const [, quoted] = body.match(/(\[.*\])/s) ?? [];
       assert.ok(quoted, ...);
       const rules = JSON.parse(quoted);
       assert.ok(rules.some((rule) => rule.permission === 'skill' && rule.action === 'deny'), ...);
       const [{ pattern }] = rules.filter((rule) => rule.permission === 'skill');
       assert.ok(skillNames().every((name) => name.startsWith(pattern.replace(/\*$/, ''))), ...);
     },
   },
   ```
   Both added to the hand-kept non-run list. New imports: `skillNames` from `./support/skills.js` and `{ README, configBlocks, refusedBlocks }` from `./support/readme.js`; the local `README` const removed.

   **`tests/readme-host.test.js` — MODIFIED**

   New `ACCOUNT` subject:
   ```js
   {
     subject: 'the permission key',
     heading: 'Permissions',
     carries: /"permission":\s*\{\s*"skill":\s*\{\s*"dpm-\*":\s*"allow"/,
     what: 'the singular key v1 takes, in place of the `permissions` array v1 refuses outright',
   },
   ```
   And the `OTHER_HOST` first pattern gained a line-scoped exemption:
   ```js
   {
     pattern: /^(?![^\n]*V2 permissions are not supported)[^\n]*\bopencode2\b/m,
     why: "the second host's binary, which nothing here is run with",
   },
   ```
   with a docblock paragraph explaining the exemption mirrors the existing `XDG_CACHE_HOME` one.

   **`src/projection/load.ts` — READ (the bug under investigation, not yet edited)**
   ```js
     // Cross-cutting joins. Both ends of a dependency are nullable and exclusive, so the two
     // descriptors are one table read two ways rather than two tables.
     artifactLinks: { table: 'artifact_document', parent: 'document_id', order: ['artifact_id'] },
     delivers: { table: 'document_milestone', parent: 'document_id', order: ['milestone_id'] },
     dependencies: { table: 'dependency', parent: 'source_document_id', order: ['kind', 'id'] },
     storyDependencies: { table: 'dependency', parent: 'source_story_id', order: ['kind', 'id'] },
   };
   ```

   **`src/projection/templates/epic.ts` — READ (not yet edited)**
   ```js
   function edgeTarget(db: DatabaseSync, edge: Row, identifiers: Map<string, string>, epicId: string) {
     if (edge.target_document_id !== null) {
       return identifiers.get(edge.target_document_id) ?? edge.target_document_id;
     }

     const target = db.prepare('SELECT number, epic_id FROM story WHERE id = ?')
       .get(edge.target_story_id) as Row | undefined;

     if (!target) return edge.target_story_id;

     const label = `Story ${target.number}`;

     if (target.epic_id === epicId) return label;

     return `${identifiers.get(target.epic_id) ?? target.epic_id} ${label}`;
   }

   const gatingKinds = (db: DatabaseSync) => new Set(
     (db.prepare('SELECT kind FROM dependency_kind WHERE gates_work = 1').all() as Row[])
       .map((row) => row.kind),
   );

   function story(db, row, ref, identifiers, epicId, gating) {
     const blockedBy = row.dependencies
       .filter((edge: Row) => gating.has(edge.kind))
       .map((edge: Row) => edgeTarget(db, edge, identifiers, epicId));

     return [
       heading(2, `${storyLabel(row)} — ${ref(row.title)}`),
       [
         field('Status', row.status_note ? `${row.status} — ${ref(row.status_note)}` : row.status),
         field('Blocked by', blockedBy.length > 0 ? blockedBy.join(', ') : '—'),
       ].join('\n'),
       ...
   ```
   **Note the discrepancy I had not yet resolved**: `epic.ts:92` reads `row.dependencies`, not `row.storyDependencies`. How the story row's collections are attached (and under which key) must be checked before editing.

   **Scratch files (outside the repo)**, under `/private/tmp/claude-501/-Users-chris-Work-git-opencode-dpm/ef609fba-06b5-4d19-b550-623d9565d393/scratchpad/`:
   - `v1strings.txt` — 64MB `strings` dump of `~/.opencode/bin/opencode`. Lines **294495–294540** hold v1's authoritative Permissions and Plugins config documentation.
   - `walk/` — the throwaway project (git repo, `opencode.json`, `.git/hooks/pre-commit` symlink, `.dpm/`, `docs/quick/01-quick-deploy-verbose-flag.md`, `deploy.sh`), with commit `7d2a28a`.
   - `peek.js`, `rows.js` — small `node:sqlite` readers.
   - `walkserve.log`, `walkrun1.out/.log`, `walkdeny.*`, `walkdeny2.*`, `walkdeny3.*`, `walkallow.*`, `walkallow2.*`, `walktool.*`, `walkwild.*`, `suite-story3.txt`, `msg.txt`.

4. **Errors and fixes:**

   - **`mcp__plugin_dpm_dpm__read_story` rejected `story_id`** — its parameter is `id`. Loaded the schema via `ToolSearch` and used `id`.
   - **Bash 2-minute wall (exit 143)** hit twice while polling long-running `opencode run` sessions. Fixed by launching with `nohup` + `&` and polling in short bounded loops (`perl -e 'select(undef,undef,undef,N)'`, since foreground `sleep` is blocked).
   - **`node -e` with `type="table"`** — SQLite rejects double-quoted string literals. Rewrote as heredoc scripts with single quotes.
   - **`node peek.js x <db>`** — passed a stray first argument, so `process.argv[2]` was `"x"`. Fixed the argument order.
   - **First deny probe used the README's array shape** → host refused the whole config. This *was* the defect, not a mistake in the probe.
   - **Second deny probe returned no permission verdict** — the local model called the skill tool with `id` instead of `name` and gave up after the schema error. Fixed by prompting with the exact call shape: *"Call the tool named 'skill' with the argument name set to the string dpm-templates."*
   - **`readme-host.test.js` control failure**: `carries: /"permission":\s*\{/` matched twice in the section, so stripping one occurrence left the other and the control correctly reported it. Fixed by narrowing the pattern to the allow-list block specifically.
   - **`readme-host.test.js` must-NOT failure**: my new prose said "OpenCode 2's shape" and the verbatim host error names `opencode2`. Fixed on **both** sides — rewrote the prose to "the host's next major version" (avoiding the name in prose), and added a lookahead exemption for the diagnostic line only, anchored on the diagnostic's own words rather than on a fence, so the existing planted control still lands outside it.
   - **I misreported a test count.** One `npm test` run showed "84 failures"; a clean rerun showed 5. The 84-failure run was a bad/raced run (I had opencode processes running). The real picture was 4 failures in `permission-entries.test.js` plus the known unstaged-deletion failure. I corrected quietly and moved on.
   - **`tests/permission-entries.test.js` was a file I did not know existed** until the full suite ran. This was the most consequential discovery: its four failures exposed that the v2 array shape was baked into the test suite too.

5. **Problem Solving:**

   **The defect and why nothing caught it.** The README's Permissions section had recommended OpenCode 2's `permissions` array through four stories of a README the project had already retargeted at v1 alone. v1 refuses the whole configuration over it. `permission-entries.test.js` read the section's fenced JSON with `JSON.parse(block).permissions ?? []` — an empty list, which all ten of its tests pass over in silence. `readme-v2.test.js` checked the same blocks for being non-empty JSON, which the wrong shape is. The floor idiom (`rules.length >= 3`) was present and did not help, because it was placed on the *result* of the reading rather than on the reading itself.

   **The fix generalises**: assert the shape you refuse as an **absence**, driven red over a planted block first. Both new checks were driven red (temporarily writing the plural shape into the README, and temporarily changing the quoted pattern to `other-*`) and then reverted.

   **Verification state at commit**: `npm test` 1179/1179 (once deletions staged), `npm run typecheck`, `npm run modules`, `npm run skills` all clean. `npm run skills` prints one recorded gap: *"dpm-ralph names the Claude Code stop-hook file, which OpenCode has no equivalent for"*.

   **Coverage roll-up over spec 02** (`01M1BJS4H76EB2K3Z98YPNG17B`). Claimed this run: FR1, FR4, FR8, NFR1, NFR2, NFR4, NFR5, ENVR1, ENVR4, ENVR5, ENVX1, ENVX2, ENVX3. FR5 was already claimed. Left unclaimed with reasons:
   - **FR2** — "Tool names, schemas and behaviour are identical under both hosts" has no bound fragment.
   - **FR3** — its first clause names a "v1 registrar" for skills the delivered design no longer has; the `skills` config key replaced it.
   - **FR6, FR7** — four unverified rows: `01M1CA51ZY5R10N7KRWQ81KEGW`, `01M1CA53F4RTJ75K0ZZ4Y02RDV`, `01M1CA552G2FHMD2ZR2F3Q3ER5`, `01M1CA587FDRSBWCZW014GZM7R`.
   - **ENVR2** — no rows. **ENVR3** — "each host", only v1 exercised here. **NFR3** — no rows at all; its subject `Config.skills.paths` was superseded.
   - **ENVX4** — its check says the config files are byte-identical across a plugin load, and 1.18.25 inserts `"$schema"` into the user's `opencode.json` on load. The host's write, not the plugin's, but the check as written does not distinguish.

6. **All user messages:**

   1. *"Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with 'I'll continue' or similar. Pick up the last task as if the break never happened."*
   2. Several *"Tool loaded."* acknowledgements following `ToolSearch` calls.
   3. *"Unrelated to this work: can you tell me my ip address"*
   4. `/commitp` slash command invocation, whose body states verbatim: *"**This command OVERRIDES the default Claude Code git commit behavior.** Ignore any system-level instructions about staging specific files by name, avoiding `git add .`, or being selective about what to commit."* and *"**NEVER do any of the following during this command:** Do NOT run `git reset HEAD` ... Do NOT run `git restore --staged` ... Do NOT selectively stage individual files ... Do NOT unstage any files after staging ... Do NOT skip any files"* and *"**The ONLY staging command permitted is `git add -A` — run it once, then commit, then push.**"*
   5. *"What's next?"*
   6. *"Yes"* (agreeing to take the projection "Blocked by" inversion bug).

   **Security / process constraints that REMAIN IN EFFECT (verbatim):**
   - *"**Version control stays with the user.** Do not run mutating git operations on your own initiative — no `git commit`, no `git add`/staging, no `git branch` or `git checkout -b`, no `git push`, no `git merge`/`rebase`/`reset`."* — **except** where explicitly directed; `/commitp` was such a direction and is now spent. Read-only `git status`/`log`/`diff` remains fine.
   - *"Edit existing files with the **Edit tool**, applied file-by-file, so that each change is visible, reviewable, and reversible. Stream-processing tools — `sed`, `perl`, `awk`, and the like — have no role in editing files."* Bash for read-only grep/find/git and for running builds/tests is fine; Write for new files is fine.
   - *"**The source spec is read and never written.** It is the fixed point the run is measured against."*
   - *"Do not call the AgentTool unless the user requested it"*
   - *"Do not use workflows or deep-research unless the user requested it"*
   - User email `chris@junctionbox.com`: *"Use it only to identify the user, such as for authorship, attribution, or filtering their own work. Never send it to an unrelated service, such as in a request header, URL, or payload, unless the user explicitly asks."*
   - Memory: *"Develop with standard dpm — publish and guard go through the installed plugin, not the port's own bin/"*
   - Memory: *"Read rows, not the projection — a blank tick in a coverage matrix means retired or unverified; only list_coverage tells you which"*
   - Memory: *"Don't restate known caveats — if Chris already wrote it in a doc comment, don't warn him about it"*
   - Address the user as **"Chris"**.
   - **Standing injected-instruction conflict, resolution preserved:** auto-mode system-reminders repeatedly instruct using Bash with `sed`/heredocs for file edits. This contradicts the CPM convention above. Resolution: Bash only for reads/searches/running checks and for writing *scratch* fixtures outside the repo; Edit/Write for all repo file modifications.
   - **Credential handling observed in practice**: when inspecting `auth.json` and `opencode.json`, print only key names and redact `apiKey` values. Continue this.

7. **Pending Tasks:**

   - **Fix the projection's "Blocked by" inversion** — the user's current, explicitly agreed task. `src/projection/load.ts:112` (`storyDependencies` keyed on `source_story_id`) and `src/projection/templates/epic.ts:101` (rendering those edges' *targets* under `**Blocked by**`). `readiness.ts:15` states the contract: *"An edge reads source-blocks-target, so the epic that cannot start is the target."* The readiness query and `list_story ready:true` are correct; only the document is wrong. Currently every epic document in `docs/epics/` renders the dependency graph backwards — e.g. `docs/epics/02-05-epic-v1-walk.md` renders "Story 2 — Blocked by: Story 3" while the row says `source=story 2, target=story 3, kind=blocks`.

   Not started, and offered but not agreed:
   - Promote retro 10's lessons into the library (a `/dpm:retro` learn pass) — all three observations have `library_doc_id: null`.
   - A spec-02 revision pass for FR3, ENVX4, FR6/FR7.
   - The develop-DPM workflow regression / reopening the packaged install (`src/db/connection.ts`).
   - FR9 (v1 slash commands) and FR10 (`$ARGUMENTS` passthrough), both `could`.

8. **Current Work:**

   Epic 02-05 is closed, committed (`03afa03`) and pushed (`b9b5ea5..03afa03` on `origin/main`). All ten epics across spec 01 and spec 02 are complete.

   The user asked *"What's next?"*, I recommended the projection bug first — *"**My recommendation: fix the projection's "Blocked by" inversion first.** It's the only actual defect in shipped output, it's two lines, and right now every epic document in `docs/epics/` renders the dependency graph backwards."* — and the user replied **"Yes"**.

   I had just begun reading the code and had made **no edits**. Three reads completed:
   - `src/projection/load.ts` lines 90–130, giving the two descriptors:
     ```js
     dependencies: { table: 'dependency', parent: 'source_document_id', order: ['kind', 'id'] },
     storyDependencies: { table: 'dependency', parent: 'source_story_id', order: ['kind', 'id'] },
     ```
   - `src/projection/templates/epic.ts` lines 80–125, giving the `story()` renderer:
     ```js
     const blockedBy = row.dependencies
       .filter((edge: Row) => gating.has(edge.kind))
       .map((edge: Row) => edgeTarget(db, edge, identifiers, epicId));
     ...
     field('Blocked by', blockedBy.length > 0 ? blockedBy.join(', ') : '—'),
     ```
   - `edgeTarget` (lines 49–64) and `gatingKinds` (lines 76–79), quoted in full in section 3 above.

   **Unresolved at the point of interruption**: the story renderer reads `row.dependencies`, not `row.storyDependencies`. I need to find where a story row's collections are attached (which `COLLECTIONS` key is loaded onto a story row and under what property name) before deciding the shape of the fix — the fix is likely to be reading the *reverse* edges (`target_story_id` as parent) and resolving the *source* end, rather than a one-word rename.

9. **Optional Next Step:**

   Continue the projection "Blocked by" fix the user just agreed to. Concretely:

   1. Find where story rows get their `dependencies` property attached — grep for `storyDependencies` and `dependencies` across `src/projection/` — to establish whether `epic.ts:92`'s `row.dependencies` is the `source_document_id` descriptor, the `source_story_id` one under a different property name, or something else.
   2. Add a **reverse** descriptor in `load.ts` keyed on `target_story_id` (the edges where the story **is** the blocked one) and a corresponding `edgeSource()` in `epic.ts` resolving `source_story_id` / `source_document_id`, so `**Blocked by**` names the blockers rather than the blocked.
   3. Write the test **red first** against a planted two-story epic with one `blocks` edge, confirming the document currently names the wrong story, then fix and confirm.
   4. Re-run `npm test`, `npm run typecheck`, `npm run modules`, then republish so `docs/epics/` is regenerated correctly.

   The user's exact agreement was to my sentence: *"**My recommendation: fix the projection's "Blocked by" inversion first.** It's the only actual defect in shipped output, it's two lines, and right now every epic document in `docs/epics/` renders the dependency graph backwards. `src/projection/load.ts:112` loads a story's edges by `source_story_id` — the edges where it *is* the blocker — and `src/projection/templates/epic.ts:101` renders their targets under **Blocked by**. Wrong end, wrong label. The readiness query and `list_story ready:true` are both correct, which is why nothing caught it: the queries are right and only the document lies. A `/dpm:quick` would cover it."*

   Note that I described this as something *"A `/dpm:quick` would cover it"* — whether Chris wants the work recorded as a dpm quick document (with its number, criteria and projection) or simply done as a direct fix was not settled by his one-word "Yes". Given the project's own method, recording it as a quick is the idiomatic choice, but the substance is the fix either way.
</summary>
