# Compact Summary (PostCompact)
**Captured**: 2026-09-01 07:35:59
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Context inherited from a prior compaction:** The session began with a summary of earlier work. The controlling context was:
- A `/dpm:do` invocation (empty args) working epic 02-02 "Skill identity" (`01M1C6DZ0WSWN6M2EWHT624AQN`) in the `opencode-dpm` repo.
- Epic 02-01 was complete, committed (`3b8fa2c`) and pushed.
- The dpm plugin's `do` skill works an epic's stories to completion in one continuous loop, stopping only at named gates.
- A prior `/commitp` invocation had overridden the no-git rule for that one commit; it's back in force now.
- Standing constraints: version control stays with the user; Edit tool not sed/perl/awk for file edits; source spec is read-never-written; no AgentTool unless requested; no workflows/deep-research unless requested; email used only for identification; address the user as "Chris".
- Memory entries: "Develop with standard dpm", "Don't restate known caveats", "Read rows, not the projection".

**Turn 1 (my first action):** I rendered the five selected retro observations verbatim with proposed dispositions and gated with AskUserQuestion.

**User message 1 (AskUserQuestion answer):** "Accept all five as Applied"

**My work after that:**
1. ToolSearch to load dpm tools.
2. Listed retros to get IDs: retro 03 = `01M1ABMF2GJVS45PYQ2H6WCVW1`, retro 06 = `01M1CVM84M7H2N1F0KRW29TXB3`.
3. Created 5 `retro_application` rows (IDs `01M1CXECCF0Q4QPGC365J2TM0A`, `01M1CXEJKX7AN11F41JQ79ZK7J`, `01M1CXES34CZ14V6CDT54KQRSH`, `01M1CXF39PENMD2VMG6RC7MWYB`, `01M1CXF9GQ8N8FJZH2AP7FYMBX`).
4. Updated session with loop contract.
5. Read story 1, its 4 tasks, its 4 criteria.
6. Explored: `skills/` (23 unprefixed dirs), front-matter names all matched dirs (bare), `src/plugin/skills.ts` (ID_PREFIX), `src/plugin/registration.ts` (embedded uses `skill.id`), `tests/support/skills.js`, `tests/corpus.test.js` NAMED, `scripts/skill-body-check.ts` RECORDED_GAP, `src/guard/index.ts` PUBLISH_SKILL.
7. Key findings: cross-references already all `dpm-` prefixed (23 distinct `id "dpm-*"` forms); descriptions already say `id "dpm-<name>"`.
8. Entered plan mode, wrote plan to `/Users/chris/.claude/plans/distributed-baking-crystal.md`, called ExitPlanMode.

**User message 2:** Plan approved (via ExitPlanMode approval).

**Story 1 execution:**
- Amended criterion 1 (`01M1C9NTWSFCSRTC5ZZ8JPER19`) and criterion 3 (`01M1C9NXXH96Y1NVCGBHR2R0KR`); created epic section `01M1DS67NEV1MNADEVD287KPBM`.
- Task 1: added `PREFIX` + tolerant `skillSource` to `tests/support/skills.js`, then `mv skills/<n> skills/dpm-<n>` ×23.
- Task 2: 23 Edit calls prefixing front-matter `name:`. Used partial `Read` (limit 3) to satisfy Edit precondition.
- Task 3: verified cross-references needed no edit; changed `scripts/skill-body-check.ts` RECORDED_GAP.skill → `'dpm-ralph'`.
- Task 4: wrote `tests/skill-identity.test.js` (7 tests), added to suite-integrity ADDED list.
- Full suite: 1127 tests, 1102 pass, 25 fail.
- Fixed `tests/guard-fix.test.js` (dropped `PUBLISH_SKILL.slice(ID_PREFIX.length)`).
- Refactoring pass (5b): `plantedEntries` now uses `discoverSkills`.
- Recorded coverage on 2 rows, observation `01M1DSMJ7B4C54SHZPQGSXCAFQ` with categories complexity-underestimates + scope-surprises. Story 1 complete.

**Story 2 execution:**
- Amended criteria `01M1C9P1H5WBA1PK784S500X4F` and `01M1C9P30R4MHSA34WCS59Z2JY` (dropped "v2 registrar"/"v2 id"); epic section `01M1DSQ7A3TBFM1P9QF3V74597`.
- Deleted `ID_PREFIX` and `DiscoveredSkill.id` from `src/plugin/skills.ts`; `registration.ts` `embedded` now `name: skill.name`; `src/guard/index.ts` `PUBLISH_SKILL = 'dpm-publish'` (literal) and dropped the `src/plugin/` import.
- Fixed 7 `skill.id` reads across 5 test files (found by grep, not by suite — 5 were in assertion messages and would have gone quiet).
- Moved 5 test files from `ID_PREFIX` to `PREFIX`.
- `tests/skill-invocation.test.js` `invocation()` no longer prepends.
- Wrote `tests/registration-prefix.test.js` (6 tests) with `DECLARES_PREFIX` regex.
- Added to suite-integrity — first in wrong alphabetical slot (between readme-symlink and readme-v2), fixed to after readme-v2.
- Coverage row verified, observation `01M1DT33X480VF1K0ZM6FK8167` (testing-gaps + codebase-discoveries). Story 2 complete.

**Story 3 execution:**
- Amended criterion 3 (`01M1C9P98GHR8C87049FQ41EP8`) from 1,093 to the pre-epic 1,120.
- Investigated the 22 failures; found 7 files hold deliberate hand-kept transcriptions.
- Raised a gate about criterion 1's "no per-file edit" clause.

**User message 3 (AskUserQuestion answer):** "What a ridiculous criterion. Just edit the files"

- Amended criterion 1 (`01M1C9P62HZJAAHF9GJ6QK5T8W`); epic section `01M1DTCJ7TVKX7F9G0FX7E5D4G`.
- `tests/support/body-reads.js`: added `prefixed()` applied to CLASSIFICATION on construction, imported PREFIX.
- `tests/body-reads.test.js`: REPORTED_CLEAN prefixed, two site keys prefixed, `consult` filter and the 9-entry `asksAndDoesNotNeed` deepEqual list prefixed.
- `tests/skill-epics.test.js` / `tests/skill-spec.test.js`: `SKILL` constants.
- `tests/skills-resume.test.js`: EXEMPT keys + the `['artifact','status','templates']` loop.
- `tests/skills-gates.test.js`: EXEMPT key + `only()` map key + two expected strings.
- `tests/skill-reference-input.test.js`: SEVEN + the `[a-z]+` → `[a-z-]+` regex.
- `tests/skill-port.test.js`: RECORDED_GAP.skill.
- `tests/corpus.test.js`: NAMED prefixed, added `asStage`, `conversionProblems` normalises, `shipped` in the facilitation roll-up, several literals, PREFIX import.
- Final suite run: **1133 tests, 1132 pass, 1 fail** — only `tests/plugin.test.js:198`, the known unstaged-rename artefact.

Then the summary request arrived.

Important details to preserve:
- Node 24 PATH prefix requirement.
- Bash cwd persistence.
- Scratchpad path.
- The dpm task/story/criterion IDs.
- Story 3's tasks 2 and 3 status: task 1 complete, task 2 (corpus transcription) done in substance but the task row not yet marked complete; task 3 (write tests for story 3) not started.

Let me double check task states: I marked `01M1C9QPJ5NJW9P7PHD4AEGJEE` (story 3 task 1) complete with a status_note. I did NOT mark `01M1C9QRK8HRM8J7DG5GE71K8A` (task 2 — corpus transcription) complete, even though the NAMED edit is done. Task 3 `01M1C9QT0TNFH0GC56SFQ6HMAR` ("Write tests for Move the suite's skill-name assumptions") not started.

Story 3 criteria: `01M1C9P62HZJAAHF9GJ6QK5T8W` (amended), `01M1C9P7HS7JNEJTZQ5W8G0N8D` (corpus list + planted extra directory control), `01M1C9P98GHR8C87049FQ41EP8` (amended count).

Story 4: `01M1C9NP5XWKJ06Y9X8512V9TR` with criteria `01M1C9PAS3ZEX9AKKG0A7NQASM`, `01M1C9PC9XAWBE63YF67SD1QZB`, `01M1C9PDQPSJK3BG0VBTFV5DXR`.
</analysis>

<summary>
1. **Primary Request and Intent:**

The active work is a continuing `/dpm:do` run (invoked with empty args in an earlier, now-compacted turn) executing **epic 02-02 "Skill identity"** (`01M1C6DZ0WSWN6M2EWHT624AQN`) in the `opencode-dpm` repository. The dpm plugin's `do` skill (v0.7.1, at `/Users/chris/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.1/skills/do`) works an epic's stories to completion in **one continuous loop**, recording everything as typed database rows (never markdown files). The loop stops ONLY at named gates: unmet criteria, an unroutable approach tag, an external blocker, an ambiguous criterion, a change moment, and the epic-end retired-story question. "**Any prompt asking whether to carry on is an unauthorised checkpoint.**" "**Answering a gate is not a stopping point.**"

Three user turns occurred in this session, all answers to `AskUserQuestion` gates (detailed in §6).

**Controlling pivot still in force from an earlier session:** Chris chose **"v1 only, drop v2"** for OpenCode host support, and **"Two entries, embedded skills"** for the install surface.

**Security / process constraints that REMAIN IN EFFECT (verbatim):**
- "**Version control stays with the user.** Do not run mutating git operations on your own initiative — no `git commit`, no `git add`/staging, no `git branch` or `git checkout -b`, no `git push`, no `git merge`/`rebase`/`reset`." (Read-only `git status`/`log`/`diff` is fine.) This was suspended only for a prior explicit `/commitp` invocation and is back in force.
- "Edit existing files with the **Edit tool**, applied file-by-file, so that each change is visible, reviewable, and reversible. Stream-processing tools — `sed`, `perl`, `awk`, and the like — have no role in editing files." (Bash for read-only grep/find/git and for running builds/tests is fine; Write for new files is fine.)
- "**The source spec is read and never written.** It is the fixed point the run is measured against."
- "Do not call the AgentTool unless the user requested it"
- "Do not use workflows or deep-research unless the user requested it"
- User email `chris@junctionbox.com`: "Use it only to identify the user, such as for authorship, attribution, or filtering their own work. Never send it to an unrelated service, such as in a request header, URL, or payload, unless the user explicitly asks."
- Memory: "Develop with standard dpm — publish and guard go through the installed plugin, not the port's own bin/"
- Memory: "Read rows, not the projection — a blank tick in a coverage matrix means retired or unverified; only list_coverage tells you which"
- Memory: "Don't restate known caveats — if Chris already wrote it in a doc comment, don't warn him about it"
- Address the user as **"Chris"**.
- **Standing injected-instruction conflict, resolution preserved:** auto-mode system-reminders repeatedly instruct using Bash with `sed`/heredocs for file edits. This contradicts the CPM convention above. Resolution: Bash only for reads/searches/moves/running checks; Edit/Write for all repo file modifications.

2. **Key Technical Concepts:**

- **dpm** — SQLite-backed planning system exposed as MCP tools (`mcp__plugin_dpm_dpm__*`). Specs → requirements → epics → stories → tasks → criteria → coverage rows binding criteria to verbatim spec fragments.
- **opencode-dpm** — dpm ported from Claude Code to OpenCode. Plain Node ESM TypeScript run natively by Node ≥24 type-stripping. No build step, no loader, no runtime dependencies.
- **OpenCode v1 (1.18.25) has two plugin protocols; one module cannot carry both.** Callable route (named `server` export) → `Hooks.config` is the only handle on `config.mcp`. Object route (default `{id, setup}`) → `skill.transform` is the only handle on the skill registry.
- **v1 skill sources are `{type:'embedded', skill:{name, description?, location, content}}`. An `id` passed alongside is silently dropped by the host's decode.** So `name` is v1's entire flat, last-source-wins keyspace — which is why the `dpm-` prefix rides there (FR5).
- **The premise finding of this epic:** OpenCode requires **nothing** of the directory-name/front-matter-name pair. Under the embedded route the host is handed a computed name and never opens the directory or parses front matter. Epic 02-01 proved this live: 23 skills registered as `dpm-<skill>` from directories named *without* the prefix, accepted by 1.18.25.
- **ADR 01-05** — a registered skill name is effectively permanent from the first publish, because renaming it breaks every invocation. This is why story 2's "same string as before" criterion is load-bearing.
- **Library lesson 04 "Never match on a string another string can contain"** — path containment is `relative(a,b).startsWith('..')`; extract the element and assert how many exist; export the phrase and match that; and crucially: "**After any rename, grep for the predicates that filter on the old form — `endsWith`, `filter`, escaped `\.js` — separately from the literal name. Those are part of the rename, and the ones that go quiet are more dangerous than the ones that break.**"
- **Library lesson 05 "Ask the host, not a test's idea of the host."**
- **dpm behaviours:** criterion `position` is unique per story; superseding a criterion cascades retirement to its coverage rows; `create_document_section` needs `document_id` + `heading` + `body` + `position`.

3. **Files and Code Sections:**

**Environment facts (essential):**
- Repo root `/Users/chris/Work/git/opencode-dpm`; branch `main`.
- **Node 24 is NOT the shell default.** Prefix every run: `export PATH="/Users/chris/Library/Application Support/Herd/config/nvm/versions/node/v24.20.0/bin:$PATH"`
- **Bash cwd persists between calls** — always `cd /Users/chris/Work/git/opencode-dpm`.
- `timeout` is NOT available on this machine.
- Commands: `npm test` (`node --test`), `npm run typecheck` (`tsc --noEmit`), `npm run modules`, `npm run skills`.
- Scratchpad: `/private/tmp/claude-501/-Users-chris-Work-git-opencode-dpm/ef609fba-06b5-4d19-b550-623d9565d393/scratchpad` (suite output saved to `suite.txt` there).
- dpm session id: `ef609fba-06b5-4d19-b550-623d9565d393`.
- Publish command (per memory, uses the *installed* plugin): `node /Users/chris/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.1/bin/dpm-publish.js`

**`skills/dpm-<name>/` ×23 (RENAMED + EDITED):** all 23 directories moved with plain `mv` (not `git mv` — staging is Chris's call) from `skills/<n>` to `skills/dpm-<n>`. Each `SKILL.md` front-matter `name:` prefixed via a separate Edit call. `description:` lines were already correct (each already ended `Invoke with the skill tool, id "dpm-<name>".`). Cross-references between bodies needed **no** edit — all 23 were already written `dpm-<name>`.

**`tests/support/skills.js` (MODIFIED — the instrument, changed first so the rename didn't strand ~40 files):**
```js
export const PREFIX = 'dpm-';

export function skillSource(name) {
  const directory = name.startsWith(PREFIX) ? name : `${PREFIX}${name}`;
  return readFileSync(join(SKILLS, directory, 'SKILL.md'), 'utf8');
}
```
with a long doc comment explaining the one-directional tolerance and that `skillNames()` (which reads the tree) began yielding prefixed names on its own.

**`scripts/skill-body-check.ts` (MODIFIED):** `const RECORDED_GAP = { skill: 'dpm-ralph', pattern: /`?\.claude\/ralph-loop\.local\.md`?/g };` — with a doc comment noting this is the "goes quiet" case from library lesson 04.

**`src/plugin/skills.ts` (MODIFIED — story 2's core):** `ID_PREFIX` deleted. `DiscoveredSkill.id` deleted (doc comment: "**`id` came out with `ID_PREFIX`, and it was the same field twice.**"). `discoverSkills` now returns `[{ name, ...description, location, content }]` with no composed id. Header rewritten to explain the prefix now originates on the tree.

**`src/plugin/registration.ts` (MODIFIED):** `embedded` now `name: skill.name` (was `skill.id`), with a paragraph "**Nothing here composes that prefix, and epic 02-02 story 2 is why.**"

**`src/guard/index.ts` (MODIFIED):** `export const PUBLISH_SKILL = 'dpm-publish';` (was `` `${ID_PREFIX}publish` ``); the `import { ID_PREFIX } from '../plugin/skills.ts';` was removed — this was `src/guard/`'s only edge into `src/plugin/`.

**`tests/skill-identity.test.js` (NEW — 7 tests, all pass):** covers story 1's four criteria. Key pieces:
```js
const CORPUS = 23;
const NAME = /^[a-z0-9]+(-[a-z0-9]+)*$/;
const declared = () => skillNames().map((directory) => ({
  directory, declared: frontMatter(skillSource(directory)).name,
}));
function shared(entries) { /* derived duplicate detection, never enumerated */ }
const plantedEntries = (root) => discoverSkills(root)
  .map((skill) => ({ directory: basename(skill.location), declared: skill.name }));
```
Tests: dpm-prefixed + declares same name (with a double-prefix check); name shape; shape-check control (rejects `dpm_do`, `dpm-Do`, `DPM-do`, `dpm-do-`, `-dpm-do`, `dpm--do`, `dpm do`); body check as a process against the real root; every `id "X"` in a body resolves to a live directory; no duplicate names; planted-collision control via `packageTree`.

**`tests/registration-prefix.test.js` (NEW — 6 tests, all pass):** covers story 2's three criteria.
```js
const DECLARES_PREFIX = /(?:const|let|var)\s+[A-Z_]*(?:PREFIX|NAMESPACE)[A-Z_]*\s*(?::[^=]+)?=\s*['"`]/;
const declarations = (directory) => sweepSourcesUnder(directory)
  .filter(({ text }) => DECLARES_PREFIX.test(withoutComments(text)))
  .map(({ name }) => name);
```
Includes a **transcribed** list of the 23 published ids (the one list that must not be derived), a pairwise name/location check, the src/plugin sweep, a reintroduction control (4 spellings caught, 5 innocent lines including real doc comments left alone), and a `packageTree` control where directory and declared name deliberately differ.

**`tests/support/body-reads.js` (MODIFIED):** imports `PREFIX`; `CLASSIFICATION` (~100 keys of form `'<skill> · <tool> · <step>'`) keys stay written bare and are prefixed on construction:
```js
}).map(([name, judgement]) => [prefixed(name), judgement]));

function prefixed(name) {
  const [file, ...rest] = name.split(' · ');
  return file.includes('/') || file.startsWith(PREFIX)
    ? name
    : [`${PREFIX}${file}`, ...rest].join(' · ');
}
```
(The `shared/skill-conventions.md` entries are told apart by the slash in their own name.)

**`tests/corpus.test.js` (MODIFIED):** `NAMED` list prefixed; `PREFIX` imported; new boundary mapper and its use:
```js
function conversionProblems(stages, directories) {
  const covered = directories.map(asStage);
  return stages.filter((stage) => !covered.includes(stage))
    .map((stage) => `CPM offers ${stage} and the dpm corpus has no skill for it`);
}
const asStage = (name) => (name.startsWith(PREFIX) ? name.slice(PREFIX.length) : name);
```
`CPM_STAGES` and `ORIGINAL = ['publish']` stay **bare** (CPM's namespace / the archive's). The facilitation roll-up got `const shipped = installed.map(asStage);` with a paragraph explaining the frozen `tests/corpus-snapshot/` archive must not be rewritten. Literals updated: `'dpm-publish'`, `'dpm-publushed'`, `'dpm-spec'`, `['dpm-ralph']`, `'dpm-clean'`.

**Other test files edited (all now green):** `tests/guard-fix.test.js` (dropped the `PUBLISH_SKILL.slice(ID_PREFIX.length)` round trip; now `assert.ok(discoverSkills(ROOT).some((skill) => skill.name === PUBLISH_SKILL), …)`), `tests/plugin-entry.test.js` (`basename` import, `skill.name`, `PREFIX`), `tests/permission-entries.test.js`, `tests/skill-invocation.test.js` (`invocation = (name) => \`Invoke with the skill tool, id "${name}".\``), `tests/v1-skills.test.js`, `tests/plugin-reload.test.js`, `tests/skill-supporting-files.test.js`, `tests/body-reads.test.js` (REPORTED_CLEAN, two site keys, `dpm-consult` filter, 9-entry deepEqual list), `tests/skill-epics.test.js` (`SKILL = 'dpm-epics'`), `tests/skill-spec.test.js` (`SKILL = 'dpm-spec'`), `tests/skills-resume.test.js` (EXEMPT keys + loop list), `tests/skills-gates.test.js` (EXEMPT key, `only()` map key, two expected strings), `tests/skill-reference-input.test.js` (`SEVEN` + the regex widened from `[a-z]+` to `[a-z-]+`), `tests/skill-port.test.js` (`RECORDED_GAP.skill = 'dpm-ralph'`).

**`tests/suite-integrity.test.js` (MODIFIED):** ADDED list gained, in alphabetical order:
```
  'readme-v2.test.js', //              01-04 story 4 — every documented block, classified and run
  'registration-prefix.test.js', //    02-02 story 2 — the name copied not composed, and the constant gone
  …
  'skill-identity.test.js', //         02-02 story 1 — the prefix on disk, in both places a skill declares it
```

**`/Users/chris/.claude/plans/distributed-baking-crystal.md` (OVERWRITTEN):** holds the approved story-1 plan.

4. **Errors and fixes:**

- **Edit tool "String to replace not found"** on `src/plugin/registration.ts` — my `old_string` said "@param skill One discovered skill" where the file said "@param skill One skill". Fixed by splitting into two smaller exact-match edits.
- **`skill.id` returning `undefined` silently after `DiscoveredSkill.id` was deleted** — 7 sites across 5 test files. Two surfaced as `[undefined, undefined, …]` in a `deepEqual`; five were `undefined` interpolated into assertion **messages**, which pass silently. Found by `grep -rn "skill\.id"`, not by the suite. All changed to `skill.name`.
- **suite-integrity ADDED-list ordering** — `registration-prefix.test.js` first placed between `readme-symlink` and `readme-v2`; `readme-*` sorts before `registration-*`. Moved after `readme-v2.test.js`.
- **`tests/skill-reference-input.test.js`'s literal-check regex `[a-z]+`** would have gone on matching nothing after the rename, reporting a correct literal list as derived. Widened to `[a-z-]+` with a comment citing library lesson 04.
- **`scripts/skill-body-check.ts`'s `RECORDED_GAP.skill === 'ralph'`** — the "goes quiet" case: the exemption would silently stop applying and 5 stop-hook references would surface as breaches of an untouched corpus. Found by the enumeration pass, before it fired.
- **`grep --include=*.ts` failed under zsh globbing** (earlier session) — re-run with quoted patterns.
- **`tests/plugin.test.js:198` "a file with a shebang is executable in the index"** — reports the 23 renamed `SKILL.md` paths as index-only. **NOT a code defect**; it is an unstaged-rename fact that clears when Chris stages the renames. The same shape appeared in epic 02-01.

5. **Problem Solving:**

Established that criterion 1's stated premise ("which is what OpenCode requires of the pair") was false, using epic 02-01's already-recorded live evidence rather than a new probe. Diagnosed and resolved three change moments by amending criteria and recording epic sections. Applied retro 03's lesson concretely by changing the instrument (`skillSource`) *before* the rename, which kept ~35 test files green through a 23-directory move. Moved the `dpm-` prefix from a registration-time constant onto the tree while pinning the emitted strings byte-for-byte. Introduced two documented namespace boundaries (`asStage` for the CPM comparison and the frozen spec-47 archive; `prefixed` for the CLASSIFICATION registry) rather than rewriting an archive or a 100-entry transcription.

**Suite progression:** 1120 (pre-epic) → 1127 tests / 25 fail (after story 1) → 1133 / 23 fail (after story 2) → **1133 tests, 1132 pass, 1 fail** (current; the one failure is the unstaged-rename artefact). `npm run typecheck`, `npm run modules` and `npm run skills` are all clean.

6. **All user messages (user-role turns only):**

- AskUserQuestion answer to the retro-consumption gate: **"Accept all five as Applied"**
- ExitPlanMode approval of the story-1 plan: "User has approved your plan."
- AskUserQuestion answer to the story-3 criterion-1 gate: **"What a ridiculous criterion. Just edit the files"**
- The final instruction to produce this summary as text only, with no tool calls.

(Several "Tool loaded." acknowledgements followed ToolSearch calls. Background task notifications and system reminders are not user input and carry no approval.)

7. **Pending Tasks:**

Continue the `/dpm:do` loop on epic 02-02:
- **Story 3** `01M1C9NM656AR8N6H2ESMCD6P6` — task 1 `01M1C9QPJ5NJW9P7PHD4AEGJEE` is **complete**; task 2 `01M1C9QRK8HRM8J7DG5GE71K8A` ("Update the corpus transcription to the prefixed names") is **done in substance but its row is not yet marked complete**; task 3 `01M1C9QT0TNFH0GC56SFQ6HMAR` ("Write tests for Move the suite's skill-name assumptions — Covers the three criteria tagged `unit`, including the planted extra-directory control and the suite-count floor") is **not started**. Then Step 5 verification, Step 5b refactoring pass, coverage rows, observation, story close.
  - Criteria: `01M1C9P62HZJAAHF9GJ6QK5T8W` (amended), `01M1C9P7HS7JNEJTZQ5W8G0N8D` ("`tests/corpus.test.js`'s transcribed name list holds the twenty-three prefixed names and still fails when the tree and the list disagree. Control: a planted extra skill directory fails it."), `01M1C9P98GHR8C87049FQ41EP8` (amended to the pre-epic 1,120 baseline).
- **Story 4** `01M1C9NP5XWKJ06Y9X8512V9TR` "Make a v1 name clash visible" (`plan: 0`) — criteria `01M1C9PAS3ZEX9AKKG0A7NQASM` (a post-registration check reporting every dpm skill whose registered entry does not resolve inside the installed package), `01M1C9PC9XAWBE63YF67SD1QZB`, `01M1C9PDQPSJK3BG0VBTFV5DXR` (must_not silent overwrite, with a planted-foreign-skill control). Retro application `01M1CXES34CZ14V6CDT54KQRSH` applies here: the check must be driven against a registry containing a foreign skill dpm did not put there, or it becomes another reading of `discoverSkills` that cannot fail.
- **Then Step 8** — close the epic, roll up coverage on spec 02's requirements, and write the retro.

Still outstanding from epic 02-01, reported to Chris and not to be silently absorbed: FR8 and ENVR1–ENVR5 remain unverified (need a live v1 session); the develop-DPM workflow regression (a guard bug now blocks its own fix); no requirement took `coverage_claimed_at`; reopening the packaged install is `src/db/connection.ts` work (`bun:sqlite` under bun).

8. **Current Work:**

Immediately before the summary request I had just completed the last edit of story 3's task-1/task-2 work and run the full suite. The final command was:

```
npm test → ℹ tests 1133 / ℹ pass 1132 / ℹ fail 1
=== failing ===
test at tests/plugin.test.js:198:1
```

The single remaining failure is `tests/plugin.test.js:198` — "a file with a shebang is executable in the index, and nothing else is", which reports the 23 renamed `skills/<name>/SKILL.md` paths as tracked-but-missing. This is an **unstaged-rename fact, not a code defect**: it clears when the renames are staged, and version control is Chris's. It is already recorded in the session `state` under `not_a_defect`.

The last edits made were to `tests/skills-gates.test.js`:
```js
    ['dpm-present ### 5. Record it is exempt and no longer needs to be'],
```
and, just before, `tests/skills-resume.test.js`:
```js
  for (const name of ['dpm-artifact', 'dpm-status', 'dpm-templates']) {
```

9. **Optional Next Step:**

Mark story 3's task 2 complete (`mcp__plugin_dpm_dpm__update_task` on `01M1C9QRK8HRM8J7DG5GE71K8A`), then work task 3 — write `tests/` coverage for story 3's three criteria, whose task description is: "**Covers the three criteria tagged `unit`, including the planted extra-directory control and the suite-count floor.**" Criterion `01M1C9P7HS7JNEJTZQ5W8G0N8D` names the control explicitly: "`tests/corpus.test.js`'s transcribed name list holds the twenty-three prefixed names and still fails when the tree and the list disagree. **Control: a planted extra skill directory fails it.**" Then run Step 5 verification, the Step 5b refactoring pass, record the coverage rows and the observation, close story 3, and continue the loop into story 4 — per the loop contract recorded in the session `state`: "This run executes EVERY story of epic 02-02 … in one continuous loop. It stops ONLY at the gates dpm:do names … A finished task, a finished story, a passing verification, a refactoring pass and a commit are NOT stopping points."
</summary>
