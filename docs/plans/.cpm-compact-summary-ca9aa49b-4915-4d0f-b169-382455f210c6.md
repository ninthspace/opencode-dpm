# Compact Summary (PostCompact)
**Captured**: 2026-09-12 17:03:29
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Segment 1 — Resumed `/dpm:retro` run (from prior context summary)**

The conversation began as a continuation. I was mid-`/dpm:retro` and had discovered:
- All 44 observations already carried a `retro_id` (gathered into retros 01–10)
- No library document scoped to `retro`
- `create_retro` needs an epic or quick parent; a pivot run has neither

I reported this and gated the parent choice with AskUserQuestion. Chris chose "Library entry, scoped to pivot (Recommended)".

I then:
- Read memory files `port-fixed-ahead-of-upstream.md` and `read-rows-not-projection.md`
- Read shared conventions sections (Session Startup, Library Check, Retro Awareness, Gate Presentation) from `/Users/chris/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.7/shared/skill-conventions.md`
- Listed library (7 docs), library scopes (14 rows: architect, do, epics, spec — none for retro/quick/all)
- Read library 07 "The walk comes first" for style (doc_type: architecture, single section, heading = title)
- Got spec 02 id: `01M1BJS4H76EB2K3Z98YPNG17B`
- Drafted and gated the library entry
- Chris approved
- Session creation failed with "Invalid params" because id `ca9aa49b-...` already existed (from the 30 Aug epics run); fixed by using suffixed id `ca9aa49b-...-retro-library`
- Created library 08 `pivot-owns-what-it-unbinds` (`01M2B0G4VYX6VH35M3DVRT0DC9`), scopes `pivot` and `epics`, one section `01M2B0GQCHV60NKZZ8Y4JW77G7`
- Published; found `{{ref:...}}` rendered as bare "02" making "The pivot on 02" read wrong; amended to "The pivot on spec {{ref:...}}"; republished

**Segment 2 — First `/commitp`**

Chris invoked `/commitp`. This slash command OVERRIDES default git behaviour:
- ONLY `git add -A` permitted; never `git reset`, `git restore --staged`, never selective staging, never unstage, never skip files
- Mandatory template: `[TITLE]` / `## Changes Made` / `## Rationale` / `## Outstanding Issues` / `## Files Modified`
- `#<n>` prefix only if branch ends in digits (branch is `main`, so none)

I staged, wrote the message to scratchpad, committed — the pre-commit guard REFUSED: `.dpm/dpm.sql` was behind the database because I'd called `update_session` after publishing. Republished, committed, pushed `7a3109d..c69312c` (12 files, 281 insertions).

**Segment 3 — "what's next?"**

I ran recon: `check_integrity` (ok, 15 checked, only advisory entry 14 with 3 deliberately-retired bindings), `list_epic` (all 10 complete), `list_quick` (quick 01 complete, retro-waived). Presented three options; Chris chose "Build NFR3's check".

**Segment 4 — `/dpm:quick` for NFR3's check**

Recon before invoking:
- `opencode` at `/Users/chris/.opencode/bin/opencode`, version **1.18.30**
- `opencode debug skill` lists all skills as JSON — the oracle
- Probe from throwaway dir with `skills: [checkout/skills]` → 54 total, 23 dpm-*, all rooted at checkout
- Control probe (no skills key) → 31 total, 0 dpm-*, 0.65s

Invoked `/dpm:quick`. Startup:
- Found 3 stale session rows; closed this session's pivot row `ca9aa49b-...-pivot-spec02-followon`
- Created session `ca9aa49b-...-quick-nfr3-check`
- Library Check for `quick` returned nothing — no doc scoped to `quick` or `all`. Read library 02, 03, 06 anyway (all about writing checks)
- Retro Awareness: retro 10 observation gave the crucial codebase discovery about `CONDITIONAL_SKIPS` and `t.skip()` patterns

Assessment:
- `tests/` has 173 files
- `tests/v1-integration.test.js` and `tests/installed-runtime.test.js` both use `V1_CLI = join(process.env.HOME ?? '', '.opencode', 'bin', 'opencode')` and `t.skip()`
- `tests/suite-integrity.test.js` has `CONDITIONAL_SKIPS` (exact set, both directions) and `ADDED` (manifest with provenance comments; count derived)
- `registeredSkills()` = `discoverSkills(packageRoot())` — dpm's own module. Its docblock already says "It is a stand-in for the host and not the host"
- `tests/skills-registered.test.js` covers FR3/FR4/FR5 but never starts a host
- `tests/support/scratch.js` has `ownedDirectory(t, prefix)`
- `tests/support/skills.js` exports `PREFIX = 'dpm-'`, `skillNames()`
- ADR 02-03 `01M1BS1S883B15Q557G93HBFNS`: "guarded by a registration-time check that turns the host silently discarding it into a loud failure" — mechanism overtaken by epic 02-05 story 2

I corrected my own unfair framing of the existing test (the author had documented the limitation) and updated the quick's document section accordingly.

Created quick 02 `01M2B10ZWR3ESKYQSTDRD7GJXT` with 5 criteria, wrote `tests/skills-route.test.js`, added `CONDITIONAL_SKIPS` and `ADDED` entries to `tests/suite-integrity.test.js`.

Key error: first run failed with `SyntaxError: Unterminated string in JSON at position 65101` — the host truncates stdout at ~64KB when piped. Fixed by writing to a file with `openSync`/`closeSync`. Initially used `var probe` inside try (hoisting), cleaned to `let probe;` before try.

Drove red 4 ways: skills→`shared/` gave `[]`; `dpm-malformed/` (no SKILL.md) failed other direction; control given the key failed; `dpm-phantom/` (well-formed) was picked up so passed correctly.

Added a machine-independent `[unit]` control per the `CONDITIONAL_SKIPS` docblock standard.

Full suite: 1184 pass / 0 fail with host; 1181 pass / 0 fail / 3 skipped without. typecheck + module sweep clean.

Closed all 5 criteria `met: true` with notes, wrote closing document section, created observation `01M2B1G4T8JNJJ7EAN7RA08M93` categorised `observation:testing-gaps`, set quick status complete, published.

**Segment 5 — Second `/commitp`**

Same guard refusal (session update after publish). Republished, committed, pushed `c69312c..54d3948` (4 files, 260 insertions). Then updated memory `develop-with-standard-dpm.md` with the "Publish last" rule.

**Segment 6 — "is this opencode dpm plugin ready to install and test in a real project?"**

I did a real walk rather than asserting:
- Node v24.20.0, opencode 1.18.30
- Fresh project with README's two keys → `opencode mcp list` showed `✓ dpm connected`
- `opencode debug skill` → 54 total, 23 dpm-*, zero diagnostics, stderr empty
- MCP stdio probe (`probe-mcp.mjs`): initialize → `dpm 0.1.0, schemaVersion 27`; 184 tools; `create_problem_brief` worked; `.dpm/dpm.db` + `.gitignore` created; server warned about missing pre-commit guard
- Installed hook; commit without publish → refused naming both files and the fix
- Published, committed successfully; `dpm.db` correctly gitignored
- Fresh clone → rebuilt db from `dpm.sql` on first tool call; brief 01 read back intact

Verdict: ready. Limits stated: walk exercised tools over stdio not a live agent session; passed partly because my shell has Node 24.

**Segment 7 — `subl .`** (bash input, no question) — brief acknowledgement.

**Segment 8 — "the README is very lengthy and confusing. i would expect some basic installation instructions."**

Analysis: 612 lines; Installation at line 70; First run 145 lines; Permissions 124 lines; ~120 lines of caveats before ~15 lines of commands.

Checked test constraints:
- `readme-host.test.js` `ACCOUNT` pins instructions to headings: Installation (clone regex `^git clone https:\/\/\S+\.git \S+$`, `"plugin":\s*\[[^\]]*src\/plugin\/index\.ts`, `"skills":\s*\[[^\]]*\/skills"`, `last-one-wins`), Permissions (`"permission":\s*\{\s*"skill":\s*\{\s*"dpm-\*":\s*"allow"`), First run (`^ln -s \S+\/hooks\/pre-commit \.git\/hooks\/pre-commit$`, `^git config core\.hooksPath$`), When the guard refuses (all three COMMANDS), Status (`epics are in \`docs/epics/\``)
- `XDG_CACHE_HOME` exactly once, in Installation, matching `/is not the install[\s\S]*XDG_CACHE_HOME/`
- `<dpm clone>` (CLONE_PLACEHOLDER from `tests/support/dpm-clone.js`) defined at/before first use; `DOCUMENTED_CLONE = '~/src/opencode-dpm'`
- `first-run.test.js`: `ln -s [^\n]*dpm\/hooks\/pre-commit [^\n]*\.git\/hooks\/pre-commit` and `dpm-publish.ts`
- `findable.test.js`: section slicing, unbolded opening paragraph then bold-led cases
- `permission-entries.test.js`: Permissions section must contain `/front matter/`, `/skills\/dpm-<skill>\/SKILL\.md/`, `Nothing DPM does reads outside the project`

Offered 3 options; Chris chose "Restructure: instructions first (Recommended)" with the preview.

Rewrote README. Three test failures:
1. `permission-entries.test.js` × 2 — I'd moved front-matter and "Nothing DPM does reads outside the project" out of Permissions. Fixed by moving short versions back.
2. `readme-host.test.js` "Status says which work is not delivered" — hardcoded `02-05-epic-v1-walk.md` as "Not delivered yet". Epic 02-05 is complete (commit 03afa03, retro 10). Asked Chris; he chose "Epic shipped, claim still open".

I updated README Status (row describes what it delivered; "Three things...are on the record" with the unwatched-session item restored) and replaced the test's hardcoded pin with a count-consistency check, driven red by planting "Five things".

Final: 1184 pass / 0 fail, typecheck clean. 608 lines; clone command at line 19, ln -s at line 85; first 227 lines actionable.

I also corrected myself: 183 tools is NOT drift — it's v0.7.0's inherited count pinned as a floor by `tool-naming.test.js`; port serves 184 because epic 02-03 added `read_shared_document`.

**Segment 9 — "where is opencode.json typically kept and can it be per project?"**

Researched:
- `opencode debug paths`: config `/Users/chris/.config/opencode`
- Built-in `customize-opencode` skill table: Project = `./opencode.json`, `./opencode.jsonc`, or `.opencode/opencode.json` (walks up from cwd to worktree root); Global = `~/.config/opencode/opencode.json` or `.jsonc` (NOT `~/.opencode/`)
- `XDG_CONFIG_HOME` relocates global config
- Merge tests: A (global only) → 23 dpm; B (project config, no skills key) → 23 dpm; C (project skills → elsewhere) → **0 dpm, but MCP server still connected**; D (`plugin: []`) → dpm survives; F (different non-empty plugin array) → dpm survives
- Key finding: project `skills` array REPLACES global wholesale; a globally-installed DPM vanishes silently from a project declaring its own skills — tools with no skills, nothing logged

Offered to add to README.

**Segment 10 — "yes, add it to Installation, and include project, global scoping information"**

Added two blocks to Installation: "Where that file goes, and DPM works from either" (with scope table) and "The two scopes merge key by key"; plus "A globally installed DPM disappears from any project that declares its own `skills`" after the last-one-wins paragraph. Suite green 1184/0.

**Segment 11 (mid-turn) — "do opencode plugins automatically update or have version numbers?"**

Researched:
- `opencode plugin <module>` — "npm module name", `-g/--global`, `-f/--force` ("replace existing plugin version"); aliases `plug`; NO `add` subcommand
- No `~/.cache/opencode/packages/` on this machine
- `customize-opencode` "## Plugins" section: entries are npm spec latest (`"opencode-gemini-auth"`), npm spec pinned (`"opencode-foo@1.2.3"`), file path (`"./local-plugin.ts"`), file URL (`"file:///abs/path/plugin.js"`), tuple form (`["opencode-bar", { "key": "val" }]`); auto-discovery in `.opencode/plugin/` or `.opencode/plugins/`
- package.json: name `opencode-dpm`, version 0.1.0, not private, `files: ['bin/','hooks/','shared/','skills/','src/']`

Answered: version numbers yes for npm specs; auto-update inferred-not-verified; none applies to DPM (file path install, `git pull`). Flagged that `opencode plugin add github:...` in the README is a stale command form.

**Segment 12 — "yes, fix that line too"**

- Grepped: nothing pins the README string (test references are to `opencode2 plugin add` in planted controls/history)
- Edited README line 91 to `**`opencode plugin opencode-dpm` is not the install**...` preserving "is not the install" → `XDG_CACHE_HOME` ordering
- Line wrapping broke; rewrapped the paragraph
- Checked line lengths (only pre-existing tables/error block over 92)
- Full suite: 1184 pass / 0 fail
- Final check: no stale command forms remain; README 634 lines; typecheck ran (`tsc --noEmit`, output cut off at the summary request)

Note: README went 612 → 608 (restructure) → 634 (after config scoping + plugin line additions).

Uncommitted work at summary time: README.md changes and tests/readme-host.test.js changes (from segments 8–12). The last commit was `54d3948`.

Security-relevant constraints: `/commitp` rules (verbatim in summary), and the standing CPM convention "Version control stays with the user" unless explicitly directed.
</analysis>

<summary>
1. **Primary Request and Intent:**

   Chronologically, the user's explicit requests were:

   a. **(Resumed)** Complete the `/dpm:retro` run — "yes, do the retro" — on spec 02's 3 September pivot, which retired nine coverage bindings and re-bound none.

   b. **`/commitp`** (twice) — a slash command that **OVERRIDES default Claude Code git behaviour**, verbatim rules: *"The ONLY staging command permitted is `git add -A` — run it once, then commit, then push."* Never `git reset HEAD`, never `git restore --staged`, never selectively stage individual files with `git add <file>`, never unstage, never skip files. Mandatory commit template: `[TITLE]` / `## Changes Made` / `## Rationale` / `## Outstanding Issues` / `## Files Modified`. Issue-number prefix `#<n>` only if the branch ends in digits (branch is `main`, so none). Then `git push origin <current-branch>`.

   c. **"what's next?"** — assess project state and recommend the next piece of work.

   d. **"Build NFR3's check"** (chosen from options) — an automated check that fails when the OpenCode `skills` config route stops working, closing spec 02's NFR3.

   e. **"is this opencode dpm plugin ready to install and test in a real project?"** — readiness assessment.

   f. **"the README is very lengthy and confusing. i would expect some basic installation instructions."** → chose **"Restructure: instructions first (Recommended)"**.

   g. **"where is opencode.json typically kept and can it be per project?"**

   h. **"yes, add it to Installation, and include project, global scoping information"**

   i. **"do opencode plugins automatically update or have version numbers?"** (sent mid-turn)

   j. **"yes, fix that line too"** — correct the stale `opencode plugin add github:...` command form in the README.

2. **Key Technical Concepts:**
   - **DPM (Data-Modelled Planning Method)**: planning artefacts as SQLite rows; markdown under `docs/` is a one-way generated projection; skills write only through typed MCP tools; pre-commit guard regenerates-and-compares.
   - **Host**: OpenCode v1 only (retargeted from v2 on 2026-08-31). Installed host is **1.18.30**; DPM built/verified against 1.18.25. Node **v24.20.0**.
   - **Install shape**: two `opencode.json` keys — `plugin` (entry file `src/plugin/index.ts`, whose `config` hook is the only handle on the MCP registry) and `skills` (directory the host walks itself). Clone install, not packaged: Node refuses to strip types under `node_modules`, and the host's bun has no `node:sqlite`.
   - **Coverage model**: `create_coverage` binds `requirement_id` + verbatim `spec_fragment` + `story_criterion_id`. A quick record carries `quick_criterion` rows, which coverage cannot bind to.
   - **`accounted_for`**: a story criterion is accounted for with a live binding or a `warrant_adr_id`.
   - **Verification decay**: amending a requirement's or criterion's `text` clears bound coverage `verified_at`/`binding_hash`. Skills must never write or clear `verified_at`.
   - **Integrity invariant 9**: `coverage.spec_fragment` must be a verbatim substring of its requirement's text while live.
   - **Library scope keywords**: documents are scoped per skill (`architect`, `do`, `epics`, `spec`, now `pivot`); none scoped to `quick` or `all`.
   - **Test-suite idioms**: `CONDITIONAL_SKIPS` (hand-kept exact set, asserted both directions), `ADDED` manifest (sorted, provenance comment per entry, count derived not hardcoded), machine-independent companion controls for files that stand down.
   - **Library lessons applied**: 02 "An absence is only an observation when something was watching" (assert the corpus before the silence), 03 "Find the oracle before writing the expectation", 06 "A check that shares its subject's error cannot fail", 07 "The walk comes first", 08 "A pivot owns what it un-binds" (new).
   - **OpenCode config scoping**: project `./opencode.json` / `./opencode.jsonc` / `.opencode/opencode.json` (walks up cwd → worktree root); global `~/.config/opencode/opencode.json` (NOT `~/.opencode/`), follows `XDG_CONFIG_HOME`. Scopes merge key-by-key; a project `skills` array **replaces** the global one wholesale.
   - **OpenCode plugin entry forms**: npm spec latest, npm spec pinned (`name@1.2.3`), file path, file URL, tuple with options; auto-discovery in `.opencode/plugin(s)/`.

3. **Files and Code Sections:**

   - **`/Users/chris/.claude/plugins/cache/ninthspace-marketplace/dpm/0.7.7/shared/skill-conventions.md`** (288 lines) — read Session Startup (lines 31–51), Library Check (52–66), Retro Awareness (67–88), Gate Presentation (89–101). Authoritative procedures the skills delegate to.

   - **Library 08 `docs/library/08-library-pivot-owns-what-it-unbinds.md`** (new, id `01M2B0G4VYX6VH35M3DVRT0DC9`, section `01M2B0GQCHV60NKZZ8Y4JW77G7`, doc_type `architecture`, scopes `pivot` + `epics`). Records the lesson that retiring a binding without re-binding leaves a requirement "quiet rather than open", and that nothing in the database reports it — a requirement with zero bindings breaks no invariant.

   - **`tests/skills-route.test.js`** (NEW, ~175 lines) — the NFR3 check. Key helper:
     ```js
     const V1_CLI = join(process.env.HOME ?? '', '.opencode', 'bin', 'opencode');

     function hostSkills(t, config) {
       const directory = ownedDirectory(t, 'dpm-skills-route-');
       writeFileSync(join(directory, 'opencode.json'), `${JSON.stringify(config, null, 2)}\n`);
       const output = join(directory, 'registry.json');
       const sink = openSync(output, 'w');
       let probe;
       try {
         probe = spawnSync(V1_CLI, ['debug', 'skill'], {
           cwd: directory, encoding: 'utf8', stdio: ['ignore', sink, 'pipe'],
         });
       } finally { closeSync(sink); }
       assert.equal(probe.status, 0, `the host would not list its skills...: ${probe.stderr}`);
       const skills = JSON.parse(readFileSync(output, 'utf8'));
       assert.ok(Array.isArray(skills) && skills.length > 0, '...');
       return skills;
     }
     const dpmNames = (skills) => skills.map((s) => s.name).filter((n) => n.startsWith(PREFIX)).sort();
     ```
     Test 1 `'the host registers exactly this tree\'s skills through the skills config key [integration]'` — stands down with `t.skip()` when no CLI; asserts `expected.length > 0`; `deepEqual(dpmNames(registered), expected)`; `withinPackage` containment; then the control probe with no `skills` key in the same test. Test 2 `'control — the readings above can come out the other way [unit]'` runs anywhere.

   - **`tests/suite-integrity.test.js`** — two additions:
     ```js
     'tests/skills-route.test.js':
       "quick 02 — the host's own skill registry, read by running `opencode debug skill`. Absent, the "
       + 'skills config route goes unexercised entirely: every other reading of the skills is over the '
       + 'tree, and the tree cannot say whether the host reached it.',
     ```
     and in `ADDED`:
     ```js
     'skills-route.test.js', //           quick 02 — the same route, read out of the host that resolves it
     ```

   - **`tests/readme-host.test.js`** — replaced the hardcoded Status pin. Removed:
     ```js
     assert.match(status, /\bNot delivered yet\b/, ...);
     assert.match(status, /^\| `02-05-epic-v1-walk\.md` \| \*\*Not delivered yet\*\*/m, ...);
     ```
     Replaced with a count-consistency check plus a docblock explaining the pin "held the false row in place":
     ```js
     const unsettled = status.slice(status.indexOf('What is not settled'));
     const claimed = unsettled.match(/\b(One|Two|Three|Four|Five)\b thing/);
     assert.ok(claimed, 'Status does not say how many unfinished things it is about to name');
     const named = ['One','Two','Three','Four','Five'].indexOf(claimed[1]) + 1;
     const sentences = unsettled.split(/(?<=\.)\s+(?=(?:\*\*)?[A-Z`])/).length;
     assert.ok(sentences > named, `Status says ${claimed[1].toLowerCase()} unfinished things and does not have room to name them`);
     ```

   - **`README.md`** — restructured (612 → 608 → 634 lines). New order: intro → **Installation** → **First run** → **Permissions** → **Status** → How it works → About the host and the runtime → What the two permission keys do → Keeping the hook healthy → When something else owns the hook → When the guard refuses → When the guard is out of date → Developing DPM itself → Coming from CPM → Licence. `TL;DR` and `Requirements` removed as sections. Clone command now line 19, `ln -s` line 85. Added a verification step (`opencode mcp list` / `opencode debug skill`). Added config-scoping table and merge behaviour, plus the "globally installed DPM disappears" hazard. Status row for `02-05-epic-v1-walk.md` now describes what it delivered; "What is not settled" restored to three items. Final edit replaced `opencode plugin add github:ninthspace/opencode-dpm` with:
     ```
     **`opencode plugin opencode-dpm` is not the install**, and the failure it produces is
     quiet. That is the packaged route — `opencode plugin <module>` takes an npm module name,
     and there is no `add` subcommand — and it unpacks the package under
     `$XDG_CACHE_HOME/opencode/packages/<specifier>/node_modules/opencode-dpm/`, where Node
     refuses to strip types from any `.ts` file — on 22 and on 24, with no flag that lifts it.
     ```

   - **`tests/support/skills.js`** — `registeredSkills()` is `discoverSkills(packageRoot())`; `PREFIX = 'dpm-'`; `skillNames()` lists directories. Its docblock already admits "It is a stand-in for the host and not the host".

   - **`tests/support/dpm-clone.js`** — `DOCUMENTED_CLONE = '~/src/opencode-dpm'`, `CLONE_PLACEHOLDER = '<dpm clone>'`.

   - **Memory `develop-with-standard-dpm.md`** — added:
     > **Publish last, after the final row is written.** Closing a skill's `session` row with `update_session` is a database write like any other, so a run that publishes and *then* does its session bookkeeping leaves `.dpm/dpm.sql` behind the database — and the guard refuses the commit… This cost two blocked commits on 2026-09-12…

4. **Errors and fixes:**

   - **`create_session` "Invalid params"** — id `ca9aa49b-...` already existed from the 30 Aug epics run. Fixed by using a suffixed id (`-retro-library`, later `-quick-nfr3-check`), matching the project's existing convention.
   - **`{{ref:}}` rendered as bare "02"** — "The pivot on 02" read wrong. Amended the section body to "The pivot on spec {{ref:...}}" and republished.
   - **Pre-commit guard refused both commits** — `.dpm/dpm.sql` behind the database because `update_session` ran after `publish`. Fixed by republishing then committing. Recorded in memory after the second occurrence.
   - **`SyntaxError: Unterminated string in JSON at position 65101`** in the new test — OpenCode truncates its stdout at ~64KB when piped (registry is ~624KB). Fixed by redirecting to a real file via `openSync`/`readFileSync`. This became the justification for the helper having no `?? []` fallback.
   - **`var probe` inside a try block** — hoisting-dependent; cleaned to `let probe;` declared before the try.
   - **Three README test failures after restructure** — two from moving required sentences out of the `Permissions` section (`/front matter/`, `/skills\/dpm-<skill>\/SKILL\.md/`, `Nothing DPM does reads outside the project`); fixed by moving short versions back. The third was the hardcoded epic-02-05 Status pin, resolved by asking the user.
   - **Line wrapping broke** after the final README edit; rewrapped to the file's ~88-char hard wrap, which matters because some checks are wrap-sensitive.
   - **Self-corrections I issued to the user**: (1) my framing of `v1-integration.test.js`'s skill count as hiding a hole was unfair — the author documented it; (2) **183 tools is not drift** — it is v0.7.0's inherited count pinned as a floor by `tool-naming.test.js`, and the port serves 184 because epic 02-03 added `read_shared_document`.

5. **Problem Solving:**
   - **Retro with no material**: all 44 observations already gathered, `dpm:pivot` writes none, and a retro needs an epic/quick parent a pivot lacks. Resolved by gating and writing the lesson straight into the library.
   - **NFR3's check**: found `opencode debug skill` as an oracle outside the pair; designed set-comparison against the tree rather than a count; control in the same test; corpus asserted non-empty before comparison. Driven red four ways.
   - **Real-project install walk**: verified server connects, 184 tools, `create_problem_brief` works, `.dpm/dpm.db` + `.gitignore` auto-created, guard refuses before publish and names the fix, commit succeeds after publish, fresh clone rebuilds from `dpm.sql`.
   - **README restructure** under heavy test constraints, mapping every pinned heading/command before moving anything.
   - **Config-scoping investigation** using `XDG_CONFIG_HOME` to avoid touching the user's real global config; discovered the silent half-install hazard (project `skills` array replaces global; MCP server stays connected, skills vanish, nothing logged).
   - **Ongoing/unresolved**: NFR3's coverage row still cannot be written (coverage binds to a `story_criterion_id`); ADR 02-03's registration-time mechanism is overtaken; FR3's "location inside the installed package" half uncovered; the Blocked-by inversion between port and installed dpm 0.7.7 persists; "plugin arrays concatenate" is inferred, not verified.

6. **All user messages:**
   - *(Session-continuation system message summarising the prior `/dpm:retro` run — not a user turn.)*
   - `/commitp` (with full command definition and OVERRIDE rules quoted above)
   - "what's next?"
   - `/commitp` (second invocation, same definition)
   - "is this opencode dpm plugin ready to install and test in a real project?"
   - `subl .` (bash input, no output)
   - "the README is very lengthy and confusing. i would expect some basic installation instructions."
   - "where is opencode.json typically kept and can it be per project?"
   - "yes, add it to Installation, and include project, global scoping information"
   - "do opencode plugins automatically update or have version numbers?" (sent mid-turn)
   - "yes, fix that line too"
   - *(AskUserQuestion answers, which are user decisions:)* retro destination → "Library entry, scoped to pivot (Recommended)"; "Write this library entry?" → "Approve"; "What do you want to pick up?" → "Build NFR3's check (Recommended)"; "Ready to execute?" → "Execute"; README scope → "Restructure: instructions first (Recommended)"; Status question → "Epic shipped, claim still open (Recommended)".
   - **Security-relevant constraints stated:** the `/commitp` OVERRIDE rules (preserved verbatim in section 1b); and the standing CPM convention that **version control stays with the user** — no self-initiated `git commit`/`add`/`branch`/`push`/`merge`/`rebase`/`reset` — unless explicitly directed, which `/commitp` was.

7. **Pending Tasks:**
   - None outstanding beyond committing the current working-tree changes; the user has not asked for a commit since `54d3948`.

8. **Current Work:**

   Immediately before this summary I completed the user's request "yes, fix that line too". I had:
   - Confirmed via `opencode plugin --help` that the command is `opencode plugin <module>` taking an "npm module name", with `-g/--global` and `-f/--force` ("replace existing plugin version"), aliases `plug`, and **no `add` subcommand**.
   - Confirmed `package.json` is `{name: 'opencode-dpm', version: '0.1.0', private: None, files: ['bin/','hooks/','shared/','skills/','src/']}`.
   - Grepped and confirmed nothing pins the README's old string (test hits were `opencode2 plugin add` in `publish-package.test.js`, `readme-v2.test.js`, planted controls in `readme-host.test.js`, and the v0.7.0 fixture dump).
   - Edited the README line, preserving the test-required `"is not the install"` → `XDG_CACHE_HOME` ordering within the Installation section.
   - Rewrapped the paragraph after the edit broke the file's hard wrap.
   - Ran the full suite: **1184 tests, 1184 pass, 0 fail**.
   - Ran the final verification: `grep -n "plugin add\|github:ninthspace" README.md` → "no stale command forms remain"; `wc -l README.md` → **634**; `npm run typecheck` (`tsc --noEmit`) was executing when the summary was requested.

   **Uncommitted in the working tree:** `README.md` (restructure + config scoping + plugin-command fix) and `tests/readme-host.test.js` (Status pin replaced). Last commit is `54d3948`; branch `main`.

9. **Optional Next Step:**

   Report the completed fix to the user — the README line now reads `**`opencode plugin opencode-dpm` is not the install**`, with `opencode plugin <module>` and the absence of an `add` subcommand stated, the suite green at 1184/0, and note that `README.md` and `tests/readme-host.test.js` remain uncommitted pending a `/commitp`. No further work should be started without the user's direction, since the last explicit request ("yes, fix that line too") is now complete.
</summary>
